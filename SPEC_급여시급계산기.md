# 척척 계산기 — 급여·시급 계산기 상세 스펙

`PLAN_척척계산기.md` 6-5절을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙, 배너 새로고침 금지 원칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

알바생·직장인이 시급/연봉을 입력해 월급, 또는 실수령액 근사치를 빠르게 확인한다. 이 계산기의 모든 세금·보험 계산은 **참고용 근사치**이며, 실제 금액은 부양가족 수·비과세 항목·간이세액표에 따라 달라질 수 있음을 화면에 항상 명시한다.

## 2. 화면 구조

1. 헤더: `< 급여·시급` (좌), `최근 계산` (우)
2. 세그먼트 탭 3개: **시급 → 월급** / **연봉 → 월급·시급** / **실수령액 계산**
3. 모드별 입력 (아래 3절)
4. 계산 결과 카드 + 근사치 안내 문구
5. 액션: `새로운 계산` / `기록 저장`
6. 화면 하단 고정 배너 광고

## 3. 모드별 계산

### 3-1. 시급 → 월급
입력: 시급, 1일 근무시간, 주 근무일수, "주휴수당 포함" 토글(기본 켬)
```
주 근무시간 = 1일 근무시간 × 주 근무일수
주급(주휴수당 제외) = 시급 × 주 근무시간
주휴수당 = 주 근무시간 ≥ 15시간 ? (주 근무시간 ÷ 40) × 8 × 시급 : 0     (토글 켰을 때만 더함)
월 예상 급여 = (주급 + 주휴수당) × 4.345
```
히어로 = 월 예상 급여, 서브 = 주급, 주휴수당

### 3-2. 연봉 → 월급·시급
입력: 연봉, 월 근무시간(기본값 209시간 — 주 40시간·주휴 포함 통상 기준, 사용자가 수정 가능)
```
월급(세전) = 연봉 ÷ 12
환산 시급 = 월급 ÷ 월 근무시간
```
히어로 = 월급, 서브 = 환산 시급

### 3-3. 실수령액 계산
입력: 세전 월급
```
국민연금 = 월급 × 4.5%
건강보험 = 월급 × 3.545%
장기요양보험 = 건강보험 × 12.95%
고용보험 = 월급 × 0.9%
4대보험 합계 = 국민연금 + 건강보험 + 장기요양보험 + 고용보험

소득세(근사) = 국세청 간이세액표를 단순화한 참고용 계단식 근사치 — 정확한 세액이 아님을 명시
지방소득세 = 소득세(근사) × 10%

실수령액(근사) = 세전 월급 − 4대보험 합계 − 소득세(근사) − 지방소득세
```
히어로 = 실수령액(근사), 서브 = 4대보험 합계, 세금 합계. 화면 상단에 "실제 세액은 부양가족 수 등에 따라 달라질 수 있는 참고용 계산이에요" 문구 고정 표시.

## 4. 검증 규칙

각 모드의 필수 입력값(시급/연봉/월급) 미입력 또는 0 이하 시 인라인 오류. 월 근무시간 0 방지.

## 5. 데이터 구조

```
CalculatorHistory (salary) {
  id, calculatorType: 'salary', title,
  mode: 'hourly-to-monthly' | 'annual-to-monthly' | 'net-pay',
  inputs: {...모드별 입력값},
  result: {...모드별 결과값},
  createdAt
}
```

## 6. 참고 프로토타입

세 모드가 모두 동작하는 인터랙티브 목업을 Artifact로 제작함.
