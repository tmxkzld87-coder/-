# 번호 조합기 (토스 미니앱) 기획서

플랫폼: Apps in Toss (앱인토스)
작성일: 2026-08-24
사업자: 육퇴못한파더 작업실 (기존 원가계산기/오늘의운세/척척계산기와 동일 사업자, workspace 77253)

> 이 문서는 브레인스토밍 세션에서 사용자와 합의한 MVP 범위를 기준으로 작성됨. "주변 복권 명당 지도"는 별도 2단계 문서로 분리(11절 참고).

---

## 1. 프로젝트 개요

- **앱 이름(가칭)**: 번호 조합기 (working title, 확정 아님)
- **appName(영문 식별자, 가칭)**: `lucky-lotto`
- **목표**: 로또 6/45·연금복권720+ 번호를 조건 필터 기반으로 조합하고, 재미 요소(행운 번호 테스트)로 매주 목~토 재방문을 유도
- **MVP 범위**: 조건부 번호 조합기 + 행운 번호 테스트. 복권 명당 지도는 제외(11절 로드맵 참고)
- **수익화**: 인앱 광고(전면형 + 배너). **사업자 등록이 완료돼야 실제 SDK 연동이 가능** — 이 워크스페이스는 2026-08-24 기준 사업자 등록 미완료 상태라, MVP는 광고 자리(placeholder)만 만들고 실제 연동은 이후 단계로 미룬다.

---

## 2. 디자인 방향

"가볍고 재미있지만 사행성을 부추기지 않는 톤". 확정 당첨을 암시하거나 과도한 기대를 유발하는 문구·연출은 쓰지 않는다 — 모든 결과 문구에 "참고용/재미로 보세요" 톤을 유지한다.

- 배경 화이트, 포인트 컬러는 골드/앰버 계열(럭키 느낌, 척척계산기의 블루와 구분)
- 카드형 레이아웃, 번호 볼(공) 형태의 시각 요소는 실제 로또 공 디자인과 혼동되지 않는 선에서 단순화

### 컬러 토큰

| 이름 | 값 | 용도 |
|---|---|---|
| Primary Amber | `#F5A623` | 포인트, 생성 버튼, 강조 숫자 |
| Dark Text | `#191F28` | 제목, 본문 |
| Secondary Text | `#8B95A1` | 설명, 보조 텍스트 |
| Divider | `#F2F4F6` | 카드 테두리, 구분선 |
| Background | `#FFFFFF` | 전체 배경 |
| Light Amber Background | `#FFF6E5` | 번호 볼 배경, 강조 카드 배경 |
| Success | `#20C997` | 필터 통과 등 긍정 상태 |
| Error | `#F04452` | 검증 오류 |

### 타이포그래피

- 시스템 기본 폰트(`-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`)
- 생성된 번호는 큰 볼 형태 + `tabular-nums`로 정렬

---

## 3. 기술 스택 & 폴더 구조

척척계산기와 동일한 Granite(React Native)/apps-in-toss 프레임워크, TypeScript, 계산 로직(`calc.ts`)과 UI(`Screen.tsx`) 분리, 테스트 동반.

```
src/
  _app.tsx
  pages/                      # createRoute 라우트 정의
    index.tsx                 # 홈(로또/연금복권 탭 + 행운번호 진입)
    lotto.tsx
    pension-lottery.tsx
    lucky-numbers.tsx
  features/
    lotto/          LottoScreen.tsx        calc.ts
    pension-lottery/ PensionLotteryScreen.tsx calc.ts
    lucky-numbers/   LuckyNumbersScreen.tsx  calc.ts
    home/            HomeScreen.tsx
  data/
    lottoHistory.json          # 빌드 시 번들되는 정적 과거 회차 데이터(4절 참고)
  components/                  # 척척계산기의 NumberInput/ResultCard/AdContainer 등과 동일한 톤으로 신규 프로젝트에 맞게 재작성(코드 공유는 안 함 — 별도 프로젝트)
    NumberBall.tsx              # 생성된 번호 1개를 공 모양으로 표시
    FilterChip.tsx
    AdContainer.tsx
  storage/
    savedNumbers.ts             # 마지막 생성 결과 로컬 저장(척척계산기 history.ts와 같은 패턴)
  theme/
    colors.ts
    typography.ts
```

---

## 4. 화면 구성

### 4-1. 홈
- 상단: "번호 조합기" 타이틀
- 세그먼트 탭: 로또 6/45 / 연금복권720+
- 하단 카드: "이번 주 행운 번호 테스트" 진입 배너

### 4-2. 로또 조건부 조합기
1. **제외할 번호**: 1~45 중 선택(볼 그리드에서 탭하여 토글)
2. **필수 포함 번호**: 1~45 중 선택(제외 번호와 중복 선택 불가)
3. **최근 N주 미출현 번호 우선**: 토글 + N주 선택(기본 10주) — 켜면 최근 N회차에 나오지 않은 번호에 가중치를 둠
4. **[번호 생성하기]** 버튼 → 화면전환 인터스티셜 광고 자리(placeholder) → 5게임(줄) 생성
5. 결과: 5줄 × 6개 번호(볼 형태), 줄마다 "다시 생성" 가능
6. 액션: "이번 주 추천 번호 저장하기" / "공유하기" — 누르기 전 하단 배너 광고 자리 노출(광고 새로고침을 버튼 클릭에 연동하지 않음 — 척척계산기 PLAN 5절 원칙과 동일)

### 4-3. 연금복권 조건부 조합기
1. **조 선택**: 1조~5조 중 선택(비워두면 랜덤 조 배정)
2. **[번호 생성하기]** → 인터스티셜 광고 자리 → 5게임 생성(조 + 6자리 숫자)
3. 결과/액션은 4-2와 동일한 패턴

### 4-4. 행운 번호 테스트
1. 입력: 생년월일(선택) 또는 단어(선택, 최소 1개는 있어야 생성 가능)
2. **[운세 보기]** → 앱 내부 생성 운세 문구 1~2줄(참고용 톤 고정 문구) + 행운 번호 5줄(로또 형식)
3. 액션은 동일(저장/공유)

---

## 5. 번호 생성 로직

```
[로또]
후보 풀 = 1~45 − 제외번호
필수 포함 번호는 결과에 항상 포함(6개를 넘으면 검증 오류: "필수 포함 번호는 5개 이하여야 해요")
가중치:
  최근 N주 미출현 토글 켜짐 → 최근 N회차 데이터에 없는 번호는 가중치 2배, 나머지는 1배
  꺼짐 → 전부 동일 가중치
가중 랜덤 추출로 (6 − 필수포함 개수)개를 중복 없이 추출 → 필수 포함 번호와 합쳐 오름차순 정렬
5게임 반복(게임마다 독립 추출, 게임 간 중복 허용)

[연금복권]
조 = 선택값 또는 1~5 중 랜덤
6자리 = 000000~999999 중 랜덤(문자열로 취급, 앞자리 0 유지)
5게임 반복

[행운 번호]
시드 문자열 = 생년월일(YYYYMMDD) + 입력 단어 (둘 다 있으면 결합, 하나만 있으면 그것만)
시드 → 결정론적 해시(예: 문자열을 32bit 정수로 누적 해시) → seed 기반 PRNG(mulberry32 등 순수 JS 구현)
같은 입력 → 항상 같은 5게임 재현
운세 문구는 해시값을 3~4개 카테고리로 나눠 고정 문구 풀에서 하나 선택(운세 API 연동 없음)
```

## 6. 데이터

- `src/data/lottoHistory.json`: 동행복권 공개 JSON 엔드포인트(`getLottoNumber&drwNo=N`)에서 최근 약 52회차(1년치)를 빌드 전에 미리 받아 정적 파일로 번들. 스키마:
  ```json
  [{ "drwNo": 1200, "drwNoDate": "2026-08-15", "numbers": [3,11,19,24,31,42], "bonusNo": 27 }]
  ```
- 매주 회차 발표 후 수동으로 최신 1회차를 추가하는 스크립트(`scripts/updateLottoHistory.ts` 등, 개발 편의용 — 프로덕션 런타임에서 네트워크 호출 없음)
- 연금복권 과거 데이터는 사용하지 않음(3절 필터 설계상 불필요)

## 7. 저장 / 공유

- `storage/savedNumbers.ts`: 마지막 생성 결과(게임 타입, 번호 목록, 생성 시각)를 로컬에 저장. 서버 전송 없음(기존 앱들과 동일 원칙)
- 공유: `@apps-in-toss/framework`의 `share()` API 재사용(원가계산기에서 이미 사용한 패턴) — OS 공유 시트를 통해 카카오톡 등으로 전달

## 8. 광고 배치 원칙

척척계산기 PLAN 5절과 동일하게:
- 화면 하단 고정 배너, 스크롤 끝에만 보이는 위치 금지
- [번호 생성하기] 클릭 시 화면전환 인터스티셜 — 매 클릭마다 띄우지 않고 빈도 제한(예: 3회 생성마다 1회)
- 배너 새로고침을 버튼 클릭에 연동 금지(광고 SDK 자체 주기에만 맡김)
- **MVP에서는 실제 광고 SDK 미연동 — `AdContainer`는 placeholder만 표시**(사업자 등록 완료 후 연동)

## 9. 검증 규칙

- 필수 포함 번호가 6개를 초과하면 오류
- 제외 번호와 필수 포함 번호가 겹치면 필수 포함이 우선(제외 목록에서 자동 제거) — 화면에 안내
- 행운 번호 테스트: 생년월일·단어 둘 다 비어있으면 "생년월일이나 단어를 입력해주세요" 오류, 생성 버튼 비활성화

## 10. 공통 컴포넌트

`NumberBall`(번호 1개 표시, 상태: 기본/제외됨/필수포함됨), `FilterChip`, `AdContainer` — 척척계산기와 별도 프로젝트라 코드는 공유하지 않지만 톤(디자인 토큰 기반, 계산 로직과 UI 분리, 테스트 동반)은 동일하게 따른다.

## 11. 확장 로드맵 (2단계, 이번 범위 밖)

**주변 복권 명당 지도**: 위치 API(토스 위치 API 또는 카카오맵 API) + "1등 당첨 이력 매장" 데이터 필요. 동행복권 쪽에 공식 공개 API가 없어 데이터 확보 방법(수동 수집/제휴/크롤링 등)을 먼저 정해야 하므로 별도 스파이크(조사) 후 진행.

## 12. 구현 순서

1. 새 프로젝트 스캐폴딩(콘솔 앱 등록 → appName/아이콘 확정 → Granite 프로젝트 생성)
2. 디자인 토큰(`theme/colors.ts`, `typography.ts`) + 공통 컴포넌트(`NumberBall`, `FilterChip`, `AdContainer`)
3. 로또 히스토리 데이터 확보 및 `data/lottoHistory.json` 생성
4. 로또 조합 로직(`calc.ts`) + 화면
5. 연금복권 조합 로직 + 화면
6. 행운 번호 테스트 로직 + 화면
7. 저장/공유 연동
8. 홈 화면(탭 전환 + 진입점) 및 라우팅 마무리
9. 모바일 UX 검증

각 단계 완료 시마다 이전 기능이 정상 동작하는지 확인 후 다음 단계로 진행한다.
