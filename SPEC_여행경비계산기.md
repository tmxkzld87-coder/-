# 척척 계산기 — 여행 경비 계산기 상세 스펙

`PLAN_척척계산기.md` 6-4절(여행 경비)을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙, 배너 새로고침 금지 원칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

여행 중 쓴 돈을 항목별로 입력하면 총 경비·인당 경비·1일 평균·항목별 비중을 한 번에 보여준다. 이 계산기는 "예산 총량 파악"이 목적이며, 인원별 실제 정산(누가 더 냈는지)은 N빵 계산기로 안내한다 — 기능을 섞지 않는다.

## 2. 화면 구조

1. 헤더: `< 여행 경비` (좌), `최근 계산` (우)
2. 설명문: "여행 경비를 한눈에 정리해보세요."
3. **여행 정보**: 인원 수(스테퍼, 기본 2) · 여행 일수(선택 입력, 비워두면 1일 평균 미표시)
4. **항목별 비용** (고정 5개 카테고리, 재료비처럼 동적 추가 아님 — 세로 리스트):
   숙박 · 교통 · 식비 · 관광/액티비티 · 기타
5. **계산 결과**: 총 경비(히어로) · 인당 경비 · 1일 평균(일수 입력 시만) · 항목별 비중 바 차트
6. 안내 배너: "인원별로 얼마씩 정산할지는 N빵 계산기에서 확인하세요" (링크성 안내, 이 화면에서 정산 기능을 만들지 않음)
7. 액션: `새로운 계산` / `기록 저장`
8. 화면 하단 고정 배너 광고

## 3. 계산 공식

```
총 경비 = 숙박 + 교통 + 식비 + 관광 + 기타
인당 경비 = 총 경비 ÷ 인원 수
1일 평균 = 총 경비 ÷ 여행 일수   (일수를 입력했을 때만 계산·표시)
항목별 비중(%) = 항목 금액 ÷ 총 경비 × 100   (총 경비 0이면 전부 0으로 처리, 표시 안 함)
```

## 4. 검증 규칙

인원 수는 스테퍼로 최소 1명 미만으로 내려가지 않도록 원천 차단. 여행 일수는 선택값이라 미입력 시 에러를 띄우지 않고 1일 평균 영역만 숨긴다. 모든 항목이 0원이면 결과 카드 대신 "비용을 입력해보세요" 빈 상태 문구를 보여준다.

## 5. 데이터 구조

```
CalculatorHistory (travel-expense) {
  id, calculatorType: 'travel-expense', title,
  headcount, days,
  categories: { lodging, transport, food, activity, etc },
  totalCost, perPerson, perDay,
  createdAt
}
```

## 6. 참고 프로토타입

실시간으로 항목별 비중 바가 갱신되는 인터랙티브 목업을 Artifact로 제작함.
