# 척척 계산기 — 배송비 계산기 상세 스펙

`PLAN_척척계산기.md` 6-6절을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

상품 단가·수량·배송비·무료배송 기준을 입력하면 최종 결제 금액과 "무료배송까지 얼마 남았는지"를 바로 보여준다. 모드 탭 없이 단일 화면으로 끝나는 가장 단순한 계산기다.

## 2. 화면 구조

1. 헤더: `< 배송비` (좌), `최근 계산` (우)
2. **상품 정보**: 상품 단가, 수량(스테퍼, 기본 1)
3. **배송 정보**: 배송비, "무료배송 기준 있음" 토글 → 켜면 무료배송 기준금액 입력 노출
4. 계산 결과 카드
5. 액션: `새로운 계산` / `기록 저장`
6. 화면 하단 고정 배너 광고

## 3. 계산 공식

```
상품 합계 = 상품 단가 × 수량
배송비 부과 여부 = 무료배송 기준을 안 썼다면 항상 부과
                 무료배송 기준을 썼다면 상품 합계 ≥ 무료배송 기준 → 미부과, 아니면 부과
총 결제 금액 = 상품 합계 + (배송비 부과 시 배송비, 아니면 0)
무료배송까지 남은 금액 = max(0, 무료배송 기준 − 상품 합계)     (무료배송 기준을 켰을 때만)
```

히어로 = 총 결제 금액, 서브 = 상품 합계, 배송비(0원 표시로 무료배송 여부를 드러냄). 무료배송 기준 미달 시 "무료배송까지 {금액} 남았어요" 안내 배너, 충족 시 "무료배송 조건을 채웠어요" 안내로 전환.

## 4. 검증 규칙

상품 단가 미입력/0 이하, 수량 0 이하(스테퍼로 최소 1 보장) 시 인라인 오류. 무료배송 기준 토글이 꺼져 있으면 관련 입력·안내를 화면에서 완전히 숨긴다.

## 5. 데이터 구조

```
CalculatorHistory (shipping-fee) {
  id, calculatorType: 'shipping-fee', title,
  unitPrice, quantity, shippingFee, freeShippingThreshold,
  productTotal, shippingCharged, finalTotal,
  createdAt
}
```

## 6. 참고 프로토타입

무료배송 기준 도달 여부에 따라 안내 배너가 실시간으로 바뀌는 인터랙티브 목업을 Artifact로 제작함.
