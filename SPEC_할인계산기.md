# 척척 계산기 — 할인 계산기 상세 스펙

`PLAN_척척계산기.md` 6-2절(할인·세일 계산)을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

정가와 할인 정보 중 아는 값만 넣으면 나머지(할인율, 할인가, 절약 금액, 최종가)를 바로 계산한다. 세 가지 상황을 하나의 화면에서 세그먼트 탭으로 전환한다.

## 2. 화면 구조

1. 헤더: `< 할인 계산` (좌), `최근 계산` (우)
2. 설명문: "정가와 할인 정보로 최종 가격을 계산해보세요."
3. **계산 방식** 세그먼트 탭(3개, `ModeTabs` 공통 컴포넌트) — 탭 전환 시 입력 필드만 바뀌고 정가 필드는 공통으로 유지된다.
   - **할인율 계산**: 정가 + 할인가를 알 때 → 할인율 역산
   - **할인가 계산**: 정가 + 할인율을 알 때 → 할인가 역산
   - **중복 할인**: 정가 + 여러 번 할인율을 순차 적용 → 최종가·실질 할인율
4. **가격 정보**: 정가(필수, 공통) + 모드별 입력 필드. 필드는 원가계산기와 동일하게 **세로로 한 줄씩** 배치한다(2열 그리드 금지).
5. **계산 결과**: 모드별로 가장 중요한 숫자를 히어로로 강조 (아래 4절)
6. 액션: `새로운 계산` / `기록 저장`
7. 화면 하단 고정 배너 광고

## 3. 모드별 계산 공식과 강조 숫자

```
공통: 정가(regularPrice) 필수, 0보다 커야 함

[할인율 계산] 입력: 정가, 할인가
  절약액 = 정가 − 할인가
  할인율(%) = 절약액 ÷ 정가 × 100
  히어로 = 할인율(%) · 서브 = 절약액
  검증: 할인가 > 정가면 "할인가는 정가보다 낮아야 해요."

[할인가 계산] 입력: 정가, 할인율
  할인가 = 정가 × (1 − 할인율 ÷ 100)
  절약액 = 정가 − 할인가
  히어로 = 할인가(원) · 서브 = 절약액
  검증: 할인율 > 100이면 "할인율은 100%를 넘을 수 없어요."

[중복 할인] 입력: 정가, 할인율 목록(N개, 순차 적용, + 할인 추가/삭제)
  최종가 = 정가 × Π(1 − 할인율_i ÷ 100)
  절약액 = 정가 − 최종가
  실질 할인율(%) = 절약액 ÷ 정가 × 100   (※ 할인율 단순 합산이 아님을 반드시 안내 — 20%+10% ≠ 30%)
  히어로 = 최종가(원) · 서브 = 실질 할인율, 절약액
  검증: 할인율 목록이 비어있으면 "할인을 추가해보세요.", 각 항목 0~100 범위, 음수 입력 방지
```

## 4. 검증 규칙

정가 미입력/0 이하, 할인가가 정가 초과(할인율 계산 모드), 할인율 100% 초과, 중복 할인에서 할인 항목 0개 — 각각 해당 입력창 아래 인라인 오류 메시지. 음수는 입력 단계에서부터 막는다.

## 5. 데이터 구조

```
CalculatorHistory (discount) {
  id, calculatorType: 'discount', title,
  mode: 'rate' | 'price' | 'stacked',
  regularPrice,
  discountedPrice, discountRate,      // 모드 A/B에서 사용
  discountRates: number[],            // 중복 할인 모드에서 사용
  finalPrice, savings, effectiveRate,
  createdAt
}
```

`calculatorType`으로 원가계산기 기록과 같은 `기록` 탭 리스트에 함께 표시되며, 필드 구성만 계산기별로 다르다.

## 6. 참고 프로토타입

실제 계산이 동작하는 인터랙티브 목업을 Artifact로 제작함. 탭 전환 시 결과가 즉시 재계산되고, 중복 할인 모드는 재료비 카드와 동일한 "+ 추가 / 삭제" 패턴을 재사용한다.
