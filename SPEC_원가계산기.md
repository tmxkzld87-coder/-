# 척척 계산기 — 원가 계산기 상세 스펙

`PLAN_척척계산기.md` 6-1절(원가·마진 계산)을 이 문서로 확장한다. 디자인 토큰/컴포넌트는 `PLAN_척척계산기.md` 2절·11절을 그대로 따른다.

---

## 1. 목표

카페/음식점/소상공인이 재료비를 입력하면 **총 원가, 1개당 원가, 원가율, 예상 이익, 목표 판매가**를 쉽고 빠르게 계산한다. 핵심 질문은 "그래서 얼마에 팔아야 하지?"이며, 결과 화면은 이 질문에 바로 답해야 한다.

## 2. 화면 구조

1. 헤더: `< 원가 계산` (좌), `최근 계산` (우)
2. 설명문: "판매가와 원가를 간편하게 계산해보세요."
3. **판매 정보**: 판매 가격(필수) · 판매 수량(기본값 1) · 부가세 포함 가격 체크박스
4. **재료비**: 동적 리스트(+ 재료 추가/삭제), 재료별 실시간 "사용 원가" 표시. 재료 1건당 입력 필드는 반드시 **구매가격 + 총 구매량(포장 전체 용량) + 1회 사용량** 3가지를 모두 받는다 — 예: 원두 20,000원짜리를 사도 "총 1kg 중 한 잔에 20g 사용"이라는 총 구매량 정보가 없으면 단가를 계산할 수 없으므로, UI 라벨을 "총 구매량"으로 명확히 표기한다. 세 필드는 **2열 그리드로 욱여넣지 않고 구매가격 → 총 구매량(+단위) → 1회 사용량(+단위) 순으로 세로로 한 줄씩 쌓는다** — 좁은 화면에서 값과 단위 선택창이 잘리지 않고, 입력창도 충분히 크게 유지된다.
5. **기타 비용**: 동적 리스트(+ 비용 추가), 포장비/컵/뚜껑/빨대 등 빠른 추가 제안
6. **계산 결과**: 1개당 원가·원가율을 가장 크게 강조, 총원가/예상매출/예상이익 보조 표시, 원가율 상태 문구
7. **고급 옵션(기본 접힘, 목표 원가율만 기본 펼침)**: 목표 원가율로 판매가 계산 · 목표 이익으로 판매가 계산 · 배달/플랫폼 수수료
8. 결과 하단: `다시 계산` / `기록 저장`

초기 화면은 판매 가격 + 재료 추가 버튼 + 기타비용 추가 버튼만 보이는 최소 상태를 유지한다(섹션 15 원칙).

## 3. 계산 공식

```
재료별 사용 원가 = 구매가격 ÷ (구매수량 × 구매단위 환산계수) × (사용량 × 사용단위 환산계수)
총 원가         = Σ(재료별 사용 원가) + Σ(기타 비용)
1개당 원가       = 총 원가 ÷ 판매 수량
예상 이익        = 판매 가격 − 1개당 원가
원가율(%)        = 1개당 원가 ÷ 판매 가격 × 100      (소수점 첫째 자리까지 표시)
예상 총 매출     = 판매 가격 × 판매 수량
예상 총 이익     = 예상 이익 × 판매 수량

목표 판매가(목표 원가율 기준) = 1개당 원가 ÷ (목표 원가율 ÷ 100)   [원단위 반올림 옵션]
권장 판매가(목표 이익 기준)   = 1개당 원가 + 목표 이익

부가세 포함가 옵션: 공급가액 = 판매가 ÷ 1.1, 부가세 = 판매가 − 공급가액  (참고용 계산 명시)

플랫폼 수수료 금액 = 판매가 × 플랫폼 수수료(%)
결제 수수료 금액   = 판매가 × 결제 수수료(%)
실제 예상 수익     = 판매가 − 1개당 원가 − 플랫폼 수수료 금액 − 결제 수수료 금액 − 배달비
```

**단위 환산계수(기준 단위=1)**: g=1, kg=1000, ml=1, L=1000, 개/장/봉/병/캔=1, 직접입력=1(사용자 정의 라벨, 계수 1). 구매단위와 사용단위가 달라도(kg 구매 → g 사용) 위 공식이 자동 환산한다.

## 4. 원가율 상태 표시 (참고용 기준)

| 구간 | 문구 |
|---|---|
| 0~30% | 좋아요 |
| 30~40% | 괜찮아요 |
| 40~50% | 원가를 확인해보세요 |
| 50%~ | 원가가 높은 편이에요 |

업종별 절대 기준이 아님을 화면에 명시한다.

## 5. 데이터 구조

```
CalculatorHistory {
  id, calculatorType, title,
  sellingPrice, quantity,
  ingredients: Ingredient[],
  additionalCosts: AdditionalCost[],
  vatIncluded, platformFee, paymentFee, deliveryFee,
  totalCost, unitCost, totalRevenue, totalProfit, profit, costRate,
  createdAt
}

Ingredient {
  id, name, purchasePrice, purchaseQuantity, purchaseUnit,
  usedQuantity, usedUnit, calculatedCost
}

AdditionalCost { id, name, amount }
```

`calculatorType` 필드를 두어 이후 다른 계산기도 동일한 기록 시스템(`storage/history.ts`)을 재사용한다.

## 6. 검증 규칙

판매 가격 미입력 / 재료명 미입력 / 구매가격·구매수량·사용량·판매수량이 0 / 숫자가 아닌 값 / 지나치게 큰 숫자 / 재료가 하나도 없는 상태 — 계산 버튼(또는 저장) 시점에 해당 입력창에 친절한 오류 메시지를 표시한다 (예: "총 구매량을 입력해주세요."). 음수 입력은 애초에 막는다.

## 7. 입력 UX

숫자 키패드, 천단위 콤마 자동 표시, 소수점 중복 방지, 음수 방지. 내부 상태는 항상 숫자 타입으로 관리하고 표시할 때만 포맷한다.

## 8. 개발 순서

1. 화면 UI 골격 2. 판매가/수량 입력 3. 재료 추가/삭제 4. 단위 선택·환산 5. 재료별 원가 계산 6. 기타 비용 7. 총원가/개당원가/원가율 8. 예상 이익 9. 목표 원가율 판매가 10. 목표 이익 판매가 11. 기록 저장 12. 배달/플랫폼 수수료 13. 부가세 14. 모바일 UX 테스트. 각 단계마다 이전 기능이 깨지지 않는지 확인.

## 9. 참고 프로토타입

이 스펙을 그대로 반영한 인터랙티브 목업(실제 계산 로직 포함)을 Artifact로 제작함. PC 구현 시 계산 공식·검증 문구·상태 임계값의 1차 레퍼런스로 사용할 것.
