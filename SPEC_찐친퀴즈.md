# 찐친 퀴즈 — 설계 스펙

- 작성일: 2026-09-23
- 플랫폼: 앱인토스 React Native 미니앱 (워크스페이스 77253)
- 콘솔 등록 예정값: title `찐친 퀴즈`, titleEn `Best Friend Quiz`, appName `jjinchin-quiz`(전역 유니크라 선점돼 있으면 변경), 카테고리 생활 > 콘텐츠(3834) > 테스트(72)
- 코드 틀: `C:\Users\User\nearby-convenience-store`(RN, 반려 수정 반영본)를 복사해 시작

## 1. 목표와 원칙

"나를 얼마나 알까?" 친구 퀴즈. 내가 나에 대한 5문제를 만들어 공유하면, 친구가 토스 안에서 바로 풀고 점수를 받는다.

- **운영비 0원**: 서버 없음, 토스 포인트 없음. 퀴즈 데이터는 전부 공유 링크 안에 담는다.
- **토스 전용 강점 활용**: 링크를 받은 친구도 토스 사용자라 설치 없이 바로 열린다.
- **바이럴 고리**: 결과 화면의 "나도 만들기" → 새 퀴즈 → 새 공유.
- 로그인 없음 → 토스로그인 약관 불필요.

1차 범위에서 **제외**: 친구 점수 모아보기/랭킹(서버 필요), 테마 여러 개, 직접 문제 작성, 전면 광고.

## 2. 화면 흐름

| 경로 | 화면 | 내용 |
|---|---|---|
| `/` | 홈 | "내 퀴즈 만들기" 버튼. 저장된 내 퀴즈가 있으면 "내 퀴즈 다시 공유하기" 버튼 추가. 하단 배너 광고 |
| `/create` | 만들기 | ① 닉네임 입력(1~8자, 앞뒤 공백 제거) ② 문제 은행에서 랜덤 5문제 표시, 문제마다 "다른 문제" 버튼(현재 5개와 겹치지 않는 문제로 교체) ③ 각 문제에서 내 답 선택 ④ 5개 모두 답하면 "완성" 활성화 |
| `/done` | 완성 | 퀴즈 코드 생성 → 기기에 저장 → "친구에게 보내기" 버튼(공유창) |
| `/play?d=코드` | 풀기 | 코드 해석 실패 시 "링크가 올바르지 않아요" + "나도 만들기". 성공 시 "OO님을 얼마나 알까?" 시작 화면 → 한 문제씩 5문제 → 결과로 이동 |
| `/result` | 결과 | 점수 + 등급 문구. 버튼 3개: "결과 공유하기", "광고 보고 틀린 문제 확인하기", "나도 만들기". 하단 배너 광고 |

**검수 반려 이력 반영(필수)**
- 화면 안에 자체 뒤로가기 버튼을 두지 않는다. 네이티브 내비게이션 바만 사용 (가까운편의점 `20260913-4` 반려 사유: 뒤로가기 버튼 중복).
- 앱 진입/화면 전환 시 자동 전면 광고 금지 (로또콕 반려 이력).
- 리워드 광고는 사용자가 버튼을 눌렀을 때만 재생하고, 버튼 문구에 "광고"를 명시한다.

## 3. 문제 은행

- 파일: `src/lib/questions.ts`. 각 문제는 `{ id: number, text: string, options: string[] }`, 보기 2~4개.
- **불변 규칙**: 문제는 **추가만** 한다. id 재사용·순서 변경·삭제·의미가 바뀌는 수정 금지 (이미 퍼진 링크가 깨짐). id는 1~255.
- **조사 처리**: 문장에 토큰을 쓴다. 풀 때와 만들 때 치환값이 다르다.

| 토큰 | 친구가 풀 때 | 내가 만들 때 |
|---|---|---|
| `{이}` | `민지님이` | `내가` |
| `{은}` | `민지님은` | `나는` |
| `{의}` | `민지님의` | `나의` |

  닉네임 뒤에 항상 "님"을 붙이므로 받침에 따른 조사 문제가 생기지 않는다.

### 초안 40문제

1. `{은}` 아침형? 올빼미형? — 아침형 / 올빼미형
2. `{이}` 제일 좋아하는 계절은? — 봄 / 여름 / 가을 / 겨울
3. 짜장면 vs 짬뽕, `{은}`? — 짜장면 / 짬뽕
4. `{은}` 여행 갈 때? — 계획 꼼꼼히 / 즉흥으로
5. `{은}` 민트초코를? — 좋아한다 / 싫어한다
6. `{이}` 스트레스 받으면 제일 먼저 하는 건? — 먹기 / 자기 / 사람 만나기 / 혼자 쉬기
7. `{이}` 제일 좋아하는 치킨은? — 후라이드 / 양념 / 간장 / 치킨 별로
8. 탕수육, `{은}`? — 부먹 / 찍먹
9. `{이}` 제일 무서워하는 건? — 벌레 / 귀신 / 높은 곳 / 무서운 거 없음
10. `{이}` 쉬는 날 주로 하는 건? — 집에서 뒹굴 / 밖에 나가기 / 운동 / 밀린 일 처리
11. `{의}` 연락 스타일은? — 답장 빠름 / 몰아서 답장 / 전화파 / 읽고 까먹음
12. `{이}` 제일 자주 마시는 건? — 커피 / 차 / 탄산 / 물
13. `{은}` 얼죽아? 뜨아? — 얼죽아 / 뜨아
14. `{은}` 강아지파? 고양이파? — 강아지 / 고양이 / 둘 다 / 둘 다 별로
15. `{이}` 로또 1등 되면 제일 먼저? — 집 산다 / 여행 간다 / 저축한다 / 일 그만둔다
16. `{은}` 노래방에서? — 마이크 안 놓음 / 탬버린 담당 / 듣기만 함 / 노래방 안 감
17. `{이}` 제일 자주 여는 앱은? — 유튜브 / 인스타 / 카톡 / 게임
18. `{은}` 영화 볼 때? — 극장파 / 집에서 OTT
19. `{이}` 제일 좋아하는 영화 장르는? — 코미디 / 로맨스 / 액션 / 공포
20. `{은}` 약속 시간에? — 일찍 옴 / 딱 맞춤 / 조금 늦음 / 많이 늦음
21. `{은}` 매운 음식을? — 잘 먹음 / 적당히 / 못 먹음
22. `{은}` 잘 때? — 완전 깜깜하게 / 무드등 켜고 / 불 켜고
23. `{이}` 더 좋아하는 여행지는? — 바다 / 산 / 도시 / 해외
24. `{은}` 물건 살 때? — 오래 고민 / 바로 지름
25. `{은}` 모임에서? — 분위기 메이커 / 조용히 구경 / 먼저 집에 감
26. `{은}` 좋아하는 반찬을? — 먼저 먹음 / 마지막에 먹음
27. `{의}` 휴대폰 배터리는 보통? — 항상 넉넉 / 20% 밑이 기본
28. `{이}` 더 좋아하는 간식은? — 단 거 / 짠 거
29. 사진 찍을 때 `{은}`? — 찍어주는 쪽 / 찍히는 쪽 / 둘 다 싫음
30. `{의}` 방 상태는? — 항상 깔끔 / 가끔 대청소 / 나만 아는 질서
31. `{이}` 우울할 때 필요한 건? — 위로의 말 / 조용히 곁에 / 맛있는 거 / 혼자 있기
32. 마감 앞에서 `{은}`? — 미리미리 / 벼락치기
33. `{이}` 제일 좋아하는 빵은? — 크루아상 / 소금빵 / 단팥빵 / 빵 별로
34. 운동, `{은}`? — 꾸준히 함 / 결심만 함 / 안 함
35. 길을 잃으면 `{은}`? — 지도 앱 / 사람한테 물어봄 / 일단 걷는다
36. `{이}` 받고 싶은 선물은? — 현금 / 손편지 / 먹을 거 / 필요한 물건
37. 친구랑 다투면 `{은}`? — 바로 풀기 / 시간 두고 풀기 / 먼저 연락 안 함
38. `{이}` 좋아하는 날씨는? — 맑은 날 / 비 오는 날 / 눈 오는 날 / 흐린 날
39. 라면은 `{은}`? — 꼬들꼬들 / 푹 퍼진
40. `{은}` E? I? — E / I / 반반

## 4. 퀴즈 코드 (링크 인코딩)

파일: `src/lib/quizCode.ts` — 순수 함수만, 단위 테스트 대상.

**바이트 구조 (형식 버전 1)**

```
[버전=1][닉네임 바이트 길이 N][닉네임 UTF-8 N바이트][문제id, 답index] × 5
```

- 문제 id 1바이트(1~255), 답 index 1바이트.
- 전체 바이트를 고정 키 `jjinchin`(ASCII)으로 반복 XOR → base64url(패딩 없음). 목적은 링크만 보고 정답을 못 읽게 하는 장난 방지이며 보안이 아니다.
- Hermes 런타임의 `TextEncoder`/`btoa` 지원 여부에 기대지 않고, UTF-8 인코딩/디코딩과 base64url을 직접 구현한다.
- 한글 8자 닉네임 기준 코드 길이 약 60자.

**해석(decode) 검증 — 하나라도 실패하면 "링크가 올바르지 않아요"**
- base64url 형식 오류 없음, 버전 == 1
- 닉네임 1~8자(공백 제거 후), 바이트 길이가 남은 데이터 안에 들어감
- 정확히 5쌍, 문제 id가 은행에 존재, 답 index < 보기 개수, 문제 id 중복 없음
- 남는 바이트 없음

## 5. 점수와 등급

파일: `src/lib/score.ts`

| 맞힌 개수 | 등급 문구 |
|---|---|
| 5 | 찐친 인정 👑 |
| 3~4 | 꽤 아는 사이 😎 |
| 1~2 | 좀 더 친해져요 🙂 |
| 0 | 처음 뵙겠습니다… 🙇 |

## 6. 공유

파일: `src/lib/share.ts`

- 퀴즈 링크: `getTossShareLink('intoss://<appName>/play?d=<코드>', OG이미지URL)` → `share({ message })`
  - 메시지: `{닉네임}님을 얼마나 알고 있나요? 5문제로 확인해보세요 👉 {링크}`
- 결과 공유: `{닉네임}님 퀴즈 {점수}/5점! {등급 문구} 너도 만들어봐 👉 {앱 홈 링크}`
- appName은 `src/lib/config.ts` 상수 한 곳에서만 관리 (콘솔 등록 결과에 따라 바뀔 수 있음).
- OG 이미지: `-` 저장소 GitHub Pages에 이미지 1장 올려서 사용. 콘솔 `image_upload_url` 이미지는 콘솔 등록 외 용도로 쓰면 삭제될 수 있어 사용 금지.

## 7. 저장

- 토스 `Storage`만 사용 (AsyncStorage는 이 환경에서 동작 안 함).
- 저장 항목은 "내가 마지막으로 만든 퀴즈 코드" 하나. 친구의 점수·답은 어디에도 저장하지 않는다.
- 결과 화면으로는 퀴즈 코드 + 친구 답(5자리 숫자 문자열)을 내비게이션 파라미터로 넘긴다.

## 8. 광고

가까운 시리즈에서 검증된 패턴을 그대로 쓴다.

- 배너: 홈·결과 화면 하단. `AdBanner` 컨테이너 높이 96 고정 + `overflow: 'hidden'`, `InlineAd`에 `impressFallbackOnMount`.
- 리워드: "광고 보고 틀린 문제 확인하기" 버튼 탭 → 광고 → `userEarnedReward` 이벤트 후 문제별로 "친구 답 / 정답" 목록 공개. 광고 로드 실패·미지원 시 버튼은 비활성 없이 다시 누를 수 있게 두고 안내 문구 표시.
- 전면 광고: 1차 범위 제외.
- 개발·테스트 빌드는 테스트 ID(`ait-ad-test-banner-id`, `ait-ad-test-rewarded-id`), `bundle_submit_review` 직전에만 실제 ID로 교체. 광고 그룹 이름은 워크스페이스 안에서 유일하게("찐친퀴즈 배너" 등), 다른 앱 광고 ID 재사용 금지.

## 9. 파일 구성

```
src/lib/questions.ts   문제 은행 + renderQuestion(text, mode, nickname)
src/lib/quizCode.ts    encodeQuiz / decodeQuiz (+ utf8, base64url, xor 헬퍼)
src/lib/score.ts       scoreAnswers / gradeFor
src/lib/share.ts       퀴즈·결과 공유
src/lib/storage.ts     내 퀴즈 코드 저장/조회
src/lib/ads.ts         광고 ID + 리워드 광고 헬퍼
src/lib/config.ts      APP_NAME, OG_IMAGE_URL
src/components/AdBanner.tsx
src/pages/index.tsx, create.tsx, done.tsx, play.tsx, result.tsx
```

## 10. 테스트

- **단위 테스트(jest)**: 인코딩→디코딩 왕복(한글·영문·이모지 닉네임), 잘못된 코드들(빈 문자열, 깨진 base64, 버전 불일치, 없는 문제 id, 답 범위 초과, 중복 id, 남는 바이트) 모두 null 반환, 점수·등급 경계값, 조사 토큰 치환.
- **정적 검사**: `tsc --noEmit`, `eslint`.
- **실기기**: 테스트 푸시 후 ① 퀴즈 만들기 → 공유 링크를 다른 계정/기기로 열어서 풀기 ② 결과 공유 ③ 리워드 광고(테스트 ID)로 틀린 문제 확인 ④ 깨진 링크 직접 열어서 에러 화면 확인 ⑤ 네이티브 뒤로가기 버튼만 보이는지 확인.
