# 척척 계산기 — 수익률 계산기 상세 스펙

`PLAN_척척계산기.md` 6-7절을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

이미 정해진 매입가·판매가로 "얼마 남는지"를 두 가지 기준(원가 대비/매출 대비)으로 빠르게 비교한다. 원가계산기(재료를 항목별로 쌓아 판매가를 설계하는 도구)와 역할이 겹치지 않도록, 이 계산기는 매입가·판매가 두 값만 받는 단순 비교 도구로 유지한다.

## 2. 화면 구조

1. 헤더: `< 수익률` (좌), `최근 계산` (우)
2. 안내문: "원가부터 판매가를 설계하려면 원가 계산기를, 이미 정한 매입가·판매가를 빠르게 비교하려면 여기를 이용하세요." (원가계산기와의 역할 차이를 화면에서 바로 인지시킴)
3. **가격 정보**: 매입가, 판매가
4. **기준 선택** 칩: 원가 대비(ROI) / 매출 대비(마진율) — 히어로에 반영될 기준만 전환, 두 값 다 항상 서브에 표시
5. 계산 결과 카드
6. 액션: `새로운 계산` / `기록 저장`
7. 화면 하단 고정 배너 광고

## 3. 계산 공식

```
수익금 = 판매가 − 매입가
원가 대비 수익률(ROI, %) = 수익금 ÷ 매입가 × 100
매출 대비 마진율(%)      = 수익금 ÷ 판매가 × 100
```

선택한 기준에 따라 히어로 라벨/값만 바뀌고, 나머지 한 값과 수익금은 항상 서브 카드에 함께 표시해 두 지표의 차이를 바로 비교할 수 있게 한다.

## 4. 검증 규칙

매입가·판매가 미입력/0 이하 시 인라인 오류. 판매가가 매입가보다 낮아 손실이 나는 경우 수익금을 음수로 그대로 표시하고 색상을 Error 톤(`#F04452`)으로 바꿔 손실임을 즉시 인지시킨다.

## 5. 데이터 구조

```
CalculatorHistory (profit-rate) {
  id, calculatorType: 'profit-rate', title,
  costPrice, sellPrice, profit, roi, marginRate,
  createdAt
}
```

## 6. 참고 프로토타입

기준 칩을 전환하면 히어로 숫자가 즉시 바뀌는 인터랙티브 목업을 Artifact로 제작함.
