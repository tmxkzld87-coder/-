# 척척 계산기 — 부가세 계산기 상세 스펙

`PLAN_척척계산기.md` 6-8절을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

공급가액 또는 부가세 포함 합계금액 중 아는 값 하나만 입력하면 나머지 두 값(공급가액/부가세/합계)을 바로 계산한다.

## 2. 화면 구조

1. 헤더: `< 부가세` (좌), `최근 계산` (우)
2. 세그먼트 탭 2개: **공급가액으로 계산** / **합계금액으로 계산**
3. 모드별 단일 입력 필드
4. 계산 결과 카드 (공급가액·부가세·합계금액 3개 항상 모두 표시, 입력한 값을 제외한 나머지가 히어로)
5. 액션: `새로운 계산` / `기록 저장`
6. 화면 하단 고정 배너 광고

## 3. 계산 공식 (부가세율 10% 고정)

```
[공급가액으로 계산] 입력: 공급가액
  부가세 = 공급가액 × 10%
  합계금액 = 공급가액 × 1.1
  히어로 = 합계금액

[합계금액으로 계산] 입력: 합계금액(부가세 포함가)
  공급가액 = 합계금액 ÷ 1.1
  부가세 = 합계금액 − 공급가액
  히어로 = 공급가액
```

세 값(공급가액/부가세/합계금액)은 모드와 무관하게 항상 결과 카드에 함께 노출한다. "일반과세 10% 기준의 참고용 계산이에요" 안내 문구를 고정 표시한다.

## 4. 검증 규칙

입력값 미입력/0 이하 시 인라인 오류.

## 5. 데이터 구조

```
CalculatorHistory (vat) {
  id, calculatorType: 'vat', title,
  mode: 'from-supply' | 'from-total',
  supplyAmount, vatAmount, totalAmount,
  createdAt
}
```

## 6. 참고 프로토타입

탭 전환 시 입력 필드와 히어로가 바뀌는 인터랙티브 목업을 Artifact로 제작함.
