# 척척 계산기 (토스 미니앱) 기획서

플랫폼: Apps in Toss (앱인토스)
작성일: 2026-08-22
사업자: 육퇴못한파더 작업실 (기존 원가계산기/오늘의운세와 동일 사업자)

> 이 문서는 확정된 상세 스펙을 기준으로 작성되었습니다. 이전 초안(`통합계산기`)은 이 문서로 대체됩니다.

---

## 1. 프로젝트 개요

- **앱 이름**: 척척 계산기
- **목표**: 생활에 필요한 각종 계산기를 하나의 앱에서 빠르고 직관적으로 사용
- **기존 서비스와의 관계**: 기존 "원가계산기" 단독 미니앱(재료·메뉴·매출 관리 포함, 더 넓은 스코프)은 그대로 유지. 척척 계산기 안의 "원가 계산" 카테고리는 **단품 원가율·마진·판매가 계산**에 집중.

---

## 2. 전체 디자인 방향

"깔끔하고 신뢰감 있는 금융 서비스 느낌". 토스의 깔끔함과 여백감은 참고하되 UI를 그대로 복제하지 않는다.

- 배경 화이트, 메인 컬러 블루
- 카드·버튼 과도한 사용 지양, 장식·큰 일러스트 없음
- 정보 밀도는 낮게, 앱을 열고 1초 안에 원하는 계산기를 찾을 수 있어야 함

### 컬러 토큰

| 이름 | 값 | 용도 |
|---|---|---|
| Primary Blue | `#3182F6` | 포인트, 강조 텍스트, 버튼 |
| Dark Text | `#191F28` | 제목, 본문, 핵심 결과 |
| Secondary Text | `#8B95A1` | 설명, 보조 텍스트, 플레이스홀더 |
| Divider | `#F2F4F6` | 카드 테두리, 구분선 |
| Background | `#FFFFFF` | 전체 배경 |
| Light Blue Background | `#EAF3FF` | 아이콘 배지 배경 |
| Success | `#20C997` | 절약/이익 등 긍정 결과 강조 |
| Warning | `#FFB020` | 주의 안내 |
| Error | `#F04452` | 입력 오류, 잘못된 값 안내 |

아이콘은 **단순 라인 스타일 + 블루 계열 단색**으로 통일한다 (카테고리별 색을 다르게 쓰지 않는다).

### 타이포그래피

- 시스템 기본 폰트 (`-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`), 한국어 가독성 최우선
- 제목: Bold / 본문: Regular
- 숫자는 `tabular-nums`로 자릿수 정렬, 결과 숫자는 크게 강조

---

## 3. 기술 스택 & 아키텍처

- **플랫폼**: Apps in Toss — React Native 기반 Granite 프레임워크
- **디자인 시스템**: TDS React Native 컴포넌트 활용 (단, 위 커스텀 컬러 토큰을 테마로 오버라이드)
- **언어**: TypeScript
- **원칙**: 계산 로직(`calc.ts`)과 UI(`*Screen.tsx`)를 분리. 계산기 목록은 데이터 기반(config)으로 관리해 신규 계산기 추가 시 홈 화면 코드를 건드리지 않도록 한다.

### 폴더 구조

```
src/
  app/                        # 진입점, 하단 탭 네비게이션(홈/기록/설정)
  screens/
    Home.tsx
    History.tsx
    Settings.tsx
  data/
    calculators.ts            # 계산기 메타데이터(id, 이름, 설명, 아이콘, 카테고리) — 데이터 기반 목록의 단일 출처
  features/
    cost/            CostScreen.tsx       calc.ts
    discount/         DiscountScreen.tsx   calc.ts
    split-bill/        SplitBillScreen.tsx  calc.ts
    travel-expense/    TravelExpenseScreen.tsx calc.ts
    salary/            SalaryScreen.tsx     calc.ts
    shipping-fee/       ShippingFeeScreen.tsx calc.ts
    profit-rate/        ProfitRateScreen.tsx calc.ts
    vat/                 VatScreen.tsx        calc.ts
  components/
    Header.tsx
    SearchBar.tsx
    CalculatorCard.tsx         # 자주 쓰는 계산 2열 카드
    CalculatorListItem.tsx     # 전체 계산기 리스트 아이템
    NumberInput.tsx
    ResultCard.tsx
    BottomNavigation.tsx
    AdContainer.tsx
    HistoryItem.tsx
  storage/
    history.ts                 # 로컬 계산 기록 저장/조회 (AsyncStorage)
    usage.ts                   # 사용 빈도/최근 사용 트래킹
  theme/
    colors.ts
    typography.ts
```

`data/calculators.ts`가 홈 화면의 "자주 쓰는 계산", "전체 계산기", 검색 대상의 단일 출처(single source of truth)가 되어야 신규 계산기 추가 시 홈 화면 UI 코드를 수정하지 않아도 된다.

---

## 4. 앱 구조 (하단 네비게이션)

**[홈] [기록] [설정]** 3탭만 사용.

- **홈**: 계산기 목록, 최근 사용, 자주 쓰는 계산기
- **기록**: 최근 계산 내역, 결과 다시 보기, 재사용
- **설정**: 앱 설정, 광고 관련 설정, 개인정보/약관, 앱 정보

---

## 5. 홈 화면 상세

### 상단
- "척척 계산기" (너무 크지 않게), 부제 "필요한 계산, 한 번에"
- 화면 상단 여백 넉넉히

### 검색
- Placeholder: "어떤 계산을 할까요?"
- 검색 아이콘 표시, 입력 즉시 실시간 필터링
- 검색 대상 예시: 원가, 할인, 더치페이, 여행, 급여, 배송비, 부가세 (계산기 이름 + 설명 텍스트 기준 매칭)

### 최근 사용
- 이전 사용 이력이 있을 때만 표시 (없으면 섹션 자체를 숨김)
- 가로 스크롤 칩 형태: `[원가 계산] [할인 계산]`
- 사용 빈도 + 최근 사용 시각을 기준으로 정렬

### 자주 쓰는 계산 (핵심 영역)
- 2열 그리드, 기본 노출 4개: **원가 계산 · 할인 계산 · N빵 계산 · 여행 경비**
- 카드 스타일: 흰 배경, `#F2F4F6` 얇은 테두리, 과하지 않은 radius, 그림자 최소화
- 아이콘: `#EAF3FF` 배지 + 블루 라인 아이콘
- 제목 `#191F28` / 설명 `#8B95A1`

### 전체 계산기
- 카드가 아닌 **리스트** 형태, 아이콘 + 제목 + 한줄 설명 + 우측 화살표
- 터치 영역을 충분히 크게, 항목 사이 매우 연한 구분선(`#F2F4F6`)
- 초기 8개 항목:

| 아이콘 | 이름 | 설명 |
|---|---|---|
| 영수증 | 원가 · 마진 | 원가율과 목표 판매가 계산 |
| % | 할인 · 세일 | 할인 후 가격 계산 |
| 인원 | 더치페이 | 인원별 금액 계산 |
| 원 | 급여 · 시급 | 월급과 시급 계산 |
| 캐리어 | 여행 경비 | 여행 총비용과 1인당 비용 계산 |
| 트럭 | 배송비 | 배송비와 총 결제금액 계산 |
| 그래프 | 수익률 | 수익률과 마진 계산 |
| 태그 | 부가세 | 공급가와 부가세 계산 |

### 광고
- 위치: 계산기 영역 → 전체 계산기 → **하단 배너 광고** (순서 고정)
- 화면 상단이나 계산 버튼 옆에는 배치하지 않음
- 전면 광고 사용 시 매 계산마다 노출하지 않고 충분한 간격을 둠

---

## 6. 카테고리별 계산 로직

### 6-1. 원가 · 마진 계산

- 입력: 재료비, 부자재비(선택), 인건비(선택), 기타비용(선택), 모드[마진율→판매가 / 판매가→마진율]
- 총원가 = 합계
- 모드 A: 판매가 = 총원가 / (1 − 마진율/100)
- 모드 B: 마진율(%) = (판매가 − 총원가) / 판매가 × 100
- 출력: 원가율(%) = 총원가/판매가×100, 판매가, 예상 이익 (결과 화면 예시와 동일: "원가율 28.3%" / "판매가 4,500원" / "예상 이익 3,200원")

### 6-2. 할인 · 세일 계산

- 모드 A: 정가+할인가 → 할인율, 절약액
- 모드 B: 정가+할인율 → 할인가, 절약액
- 모드 C: 중복 할인(예 20%+10%) → 순차 적용한 최종가, 실질 할인율

### 6-3. 더치페이 (N빵)

- 모드 A: 단순 N빵 = 총액 ÷ 인원수, 원단위 반올림 후 잔돈은 1인에게 배정
- 모드 B: 차등 정산 — 인원별 실지출 입력 → 평균 대비 초과/부족분 계산 → 그리디 매칭으로 최소 송금 내역 생성 ("A → B: N원")

### 6-4. 여행 경비

- 입력: 인원수, 여행일수(선택), 항목별 비용(숙박/교통/식비/관광/기타)
- 출력: 총 경비, 인당 경비, 1일 평균(일수 입력 시), 항목별 비중(%)
- 실제 인당 정산이 필요하면 더치페이 카테고리로 안내

### 6-5. 급여 · 시급

- 모드 A: 시급 → 월 예상 급여 (시급×1일 근무시간×주 근무일수×4.345주, 주휴수당 포함 토글)
- 모드 B: 연봉 → 월급/시급 환산
- 모드 C: 실수령액 근사 — 4대보험(국민연금 4.5%, 건강보험 3.545%, 장기요양보험 건강보험료의 12.95%, 고용보험 0.9%) + 소득세 간이 근사 차감
- 화면 내 "참고용 근사치" 안내 문구 필수

### 6-6. 배송비

- 입력: 상품 단가, 수량, 배송비, 무료배송 기준금액(선택)
- 상품 합계 = 단가×수량
- 총 결제금액 = 상품 합계 + (상품 합계 ≥ 무료배송 기준 ? 0 : 배송비)
- 무료배송 기준 입력 시 "무료배송까지 N원 남았어요" 안내 표시

### 6-7. 수익률

- 입력: 매입가(원가), 판매가
- 수익금 = 판매가 − 매입가
- 수익률(원가 대비, ROI) = 수익금/매입가×100
- 마진율(매출 대비) = 수익금/판매가×100
- 두 기준을 토글로 전환 가능하게 하여 "원가 · 마진" 카테고리와의 혼동을 줄임 (원가 계산은 비용을 항목별로 쌓아 판매가를 설계하는 도구, 수익률은 이미 정해진 매입가/판매가로 빠르게 비교하는 도구로 역할 구분)

### 6-8. 부가세

- 모드 A: 공급가액 입력 → 부가세 = 공급가액×10%, 합계 = 공급가액×1.1
- 모드 B: 합계(부가세 포함가) 입력 → 공급가액 = 합계/1.1, 부가세 = 합계−공급가액

---

## 7. 계산기 카테고리 확장 로드맵

**초기 8개**: 원가·마진, 할인·세일, 더치페이, 급여·시급, 여행 경비, 배송비, 수익률, 부가세

**향후 확장 후보** (동일한 `calculators.ts` 데이터 구조에 항목만 추가):
대출 이자, 적금, 예금 이자, 환율, 연봉, 퇴직금, 퍼센트, 단위 변환, 칼로리, 주차비, 자동차 유지비, 배달앱 수수료

신규 계산기 추가 시 홈 화면(자주 쓰는 계산/전체 계산기/검색)은 데이터만 추가하면 자동 반영되도록 설계되어 있어야 함 (섹션 3 아키텍처 참고).

---

## 8. 계산 기록 (기록 탭)

- "오늘", "어제" 등 날짜별 그룹핑
- 항목: 계산기 이름, 핵심 결과 요약(예: "판매가 4,500원 · 원가율 28.3%" / "12,000원 → 9,600원 · 20% 할인"), 시각
- 항목 탭 시 해당 계산기로 이동, 입력값 복원
- "다시 계산하기" 버튼 제공 (입력값 프리필 후 즉시 수정 가능)
- 저장 방식: 로컬(AsyncStorage)만 사용, 서버 미전송 (기존 원가계산기 개인정보처리방침과 동일 원칙)

---

## 9. 설정 탭

- 앱 설정 (예: 알림, 테마 등 필요 시)
- 광고 관련 설정 (예: 광고 제거 옵션이 있다면 여기)
- 개인정보처리방침 / 이용약관 (척척 계산기 전용 문서 신규 작성 — 기존 `cost-calc-privacy.html`/`cost-calc-terms.html` 형식 참고, 서비스명만 교체)
- 앱 정보 (버전, 문의처)

---

## 10. UX / 인터랙션 원칙

핵심 원칙: **"계산하기 위해 고민하지 않게 한다."**

모든 계산기는 `입력 → 계산 → 결과` 흐름을 기본으로 한다.

- 입력 항목 최소화, 결과는 큰 숫자로 강조 (가장 중요한 결과가 가장 크게)
- 버튼은 명확한 pressed 상태, 입력창은 명확한 focus 상태
- 숫자 입력은 숫자 키패드 우선
- 잘못된 입력에는 친절한 안내 (Error 컬러 `#F04452` 활용)
- 계산 결과는 즉시(입력과 동시에) 업데이트, 불필요한 애니메이션 없음
- 페이지 전환은 빠르고 자연스럽게, 로딩 필요 시 최소한의 로딩 UI

### 반응형 / 모바일 최적화

- 모바일 화면 최우선, 작은 화면에서도 2열 카드 유지
- 터치 영역 충분히 크게, 텍스트 잘림/겹침 없음
- 키보드가 올라와도 입력창/계산 버튼이 가려지지 않도록 처리

---

## 11. 공통 컴포넌트

`Header`, `SearchBar`, `CalculatorCard`, `CalculatorListItem`, `NumberInput`, `ResultCard`, `BottomNavigation`, `AdContainer`, `HistoryItem` — 모두 재사용 가능하게 설계하고, 계산 로직과 UI를 분리한다. 계산 결과는 정확한 숫자 처리와 반올림 규칙을 명시적으로 적용한다(예: 원단위 반올림, 소수점 첫째 자리까지의 비율 등 카테고리별 규칙을 `calc.ts`에 상수로 정의).

---

## 12. 최종 디자인 방향 요약

"토스처럼 깔끔하지만 토스를 따라 만든 앱처럼 보이지 않는 독자적인 생활 계산 도구"

화이트 배경 + 블루 포인트 + 넉넉한 여백 + 큰 숫자 + 간결한 리스트 + 최소한의 카드 + 빠른 입력. 화려한 그래디언트, 과도한 그림자, 불필요한 일러스트/애니메이션은 사용하지 않는다.

---

## 13. 구현 순서

1. 앱 기본 구조 및 하단 네비게이션
2. 홈 화면 구현
3. 검색 기능
4. 최근 사용 기능
5. 전체 계산기 리스트
6. 원가 계산기
7. 할인 계산기
8. N빵 계산기
9. 여행 경비 계산기
10. 계산 기록 (기록 탭)
11. 광고 영역 적용
12. 나머지 계산기 추가 (급여·시급 → 배송비 → 수익률 → 부가세 순 추천)

각 단계 완료 시마다 기존 기능이 정상 작동하는지 확인 후 다음 단계로 진행한다. 처음부터 모든 계산기를 한꺼번에 만들지 말고 **홈 화면 + 공통 컴포넌트 + 원가 계산기**를 먼저 완성한 뒤 동일한 구조로 나머지를 확장한다.

---

## 14. 참고 자료

- 앱인토스 개발자센터: https://developers-apps-in-toss.toss.im/
- React Native 시작 가이드: https://developers-apps-in-toss.toss.im/tutorials/react-native.html
- TDS 컴포넌트 문서: https://developers-apps-in-toss.toss.im/design/components.html
- 예제 레포: https://github.com/toss/apps-in-toss-examples

> 위 도메인은 이 세션의 네트워크 정책상 직접 열람이 막혀 있어, 최신 CLI 명령어·TDS 토큰 세부사항은 PC 세션에서 재확인 후 시작할 것.

## 15. PC 세션 시작 체크리스트

- [ ] 이 브랜치(`claude/mobile-to-pc-transfer-p4ipjo`) pull
- [ ] 개발자센터에서 최신 React Native 셋업 가이드 확인 (CLI, `appsInToss` 플러그인 옵션)
- [ ] `theme/colors.ts`에 본 문서 2절 컬러 토큰 반영
- [ ] `data/calculators.ts` 데이터 구조부터 설계 (섹션 3, 7 참고) → 홈 화면 → 원가 계산기 순으로 MVP 구현
