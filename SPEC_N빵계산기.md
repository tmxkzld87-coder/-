# 척척 계산기 — N빵 계산기(더치페이) 상세 스펙

`PLAN_척척계산기.md` 6-3절(더치페이)을 이 문서로 확장한다. 디자인 토큰/컴포넌트, 화면 하단 고정 배너 광고, `새로운 계산`/`기록 저장` 규칙, 배너 새로고침 금지 원칙은 `SPEC_원가계산기.md`와 동일하게 따른다.

## 1. 목표

모임 총무가 "얼마씩 내면 되는지"(단순 N빵) 또는 "누가 누구에게 얼마를 보내야 하는지"(차등 정산)를 바로 확인한다.

## 2. 화면 구조

1. 헤더: `< N빵 계산` (좌), `최근 계산` (우)
2. 설명문: "더치페이 금액을 빠르게 나눠보세요."
3. **계산 방식** 세그먼트 탭(2개): **단순 N빵** / **차등 정산**
4. 모드별 입력 영역 (아래 3절)
5. **계산 결과**: 모드별 히어로 숫자 + 상세 내역
6. 액션: `새로운 계산` / `기록 저장`
7. 화면 하단 고정 배너 광고

## 3. 모드별 입력·계산

### 3-1. 단순 N빵

**입력**
- 총 금액 (필수)
- 인원 수: `-`/`+` 스테퍼로 조작 (기본값 2, 최소 1)
- 반올림 단위: 없음 / 10원 / 100원 / 1,000원 칩 선택 (기본값 100원)

**계산**
```
1인당 금액(반올림 전) = 총 금액 ÷ 인원 수
1인당 금액(반올림 후) = round(반올림 전 금액 ÷ 반올림단위) × 반올림단위   (반올림단위=없음이면 반올림 전 금액 그대로, 소수점 절사)
정산 총액 = 1인당 금액(반올림 후) × 인원 수
잔돈 = 총 금액 − 정산 총액
```
- 잔돈 = 0: "정확히 나눠떨어져요"
- 잔돈 > 0(반올림으로 덜 걷힘): "1명만 {1인당 금액 + 잔돈}원, 나머지 {인원수−1}명은 {1인당 금액}원 내면 딱 맞아요"
- 잔돈 < 0(반올림으로 더 걷힘): "1명만 {1인당 금액 − |잔돈|}원, 나머지는 {1인당 금액}원 내면 {|잔돈|}원 남아요"

히어로 = 1인당 금액(반올림 후), 서브 = 총 금액 · 인원 수 · 잔돈 안내

### 3-2. 차등 정산

**입력**: 참가자 목록(이름 + 실제 지출 금액), `+ 인원 추가`로 동적 추가/삭제, 최소 2명 필요

**계산**
```
총 지출 = Σ(참가자별 지출)
평균 지출 = 총 지출 ÷ 참가자 수
참가자별 잔액 = 참가자 지출 − 평균 지출     (양수: 돌려받을 돈, 음수: 더 내야 할 돈)

정산 내역(그리디 매칭):
  받을 사람들을 잔액 큰 순, 낼 사람들을 낼 금액 큰 순으로 정렬
  두 리스트를 서로 소진될 때까지 매칭하며 min(받을잔액, 낼잔액)만큼 "낼 사람 → 받을 사람" 송금 건 생성
  → 실제 송금 횟수를 최소화한다 (participants − settled_groups 건 이하)
```

히어로 = 1인 평균 지출, 서브 = 참가자별 +받을돈/−낼돈 상태 칩, 하단에 "OOO → ◇◇◇ : N원" 정산 리스트

## 4. 검증 규칙

- 단순 N빵: 총 금액 미입력/0 이하, 인원 수 0 이하(스테퍼가 1 미만으로 못 내려가게 원천 차단)
- 차등 정산: 참가자 2명 미만, 이름 미입력(기본값 "참가자 N"으로 자동 채움), 지출 금액 음수(입력 단계에서 차단)

## 5. 데이터 구조

```
CalculatorHistory (split-bill) {
  id, calculatorType: 'split-bill', title,
  mode: 'even' | 'weighted',
  totalAmount, headcount, roundUnit, perPerson, remainder,   // 단순 N빵
  participants: { name, amount, balance }[],                  // 차등 정산
  transfers: { from, to, amount }[],                          // 차등 정산 결과
  createdAt
}
```

## 6. 참고 프로토타입

실제 그리디 정산 로직이 동작하는 인터랙티브 목업을 Artifact로 제작함. 참가자를 추가/수정하면 정산 리스트가 즉시 재계산된다.
