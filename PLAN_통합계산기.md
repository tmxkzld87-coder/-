# 통합계산기 (토스 미니앱) 기획서

시행 예정: Apps in Toss 플랫폼
작성일: 2026-08-22
사업자: 육퇴못한파더 작업실 (기존 원가계산기/오늘의운세와 동일 사업자)

---

## 1. 개요

- **목적**: 토스 사용자가 가장 자주 쓰는 실생활 계산기를 한 앱에 모아, 토스 특유의 깔끔한 UX 그대로 제공한다.
- **타겟**: 토스 앱 사용자 전반 (자영업자, 직장인, 여행객, 모임 총무 등 상황별로 카테고리 진입).
- **핵심 원칙**: "통합"은 코드/배포/디자인 시스템 레벨에서만 이루어지고, 각 계산기(카테고리)는 기능적으로 완전히 독립된 모듈로 충실하게 구현한다. 간이 버전이 아니라 각자 하나의 완성된 계산기로 취급.
- **기존 서비스와의 관계**: 기존 "원가계산기" 단독 미니앱(재료·메뉴 관리, 매출 리포트 포함)은 그대로 유지. 통합계산기 안의 "원가계산기" 카테고리는 그보다 스코프를 좁혀 **단품 원가/마진/판매가 계산**에 집중한다 (재고·매출 관리 기능 없음).

---

## 2. 기술 스택 & 아키텍처

- **플랫폼**: Apps in Toss (앱인토스) — React Native 기반 Granite 프레임워크
- **디자인 시스템**: TDS React Native (Toss Design System) 컴포넌트 사용 → 별도 UI 라이브러리 없이도 토스 네이티브 룩 확보
- **언어**: TypeScript

### 폴더 구조 (PC에서 스캐폴딩 시 목표 구조)

```
src/
  app/                     # 앱 진입점, 네비게이션 설정
  screens/
    Home.tsx               # 카테고리 그리드 홈 화면
  features/
    cost-calculator/        # 원가계산기
      CostCalculatorScreen.tsx
      calc.ts               # 순수 계산 로직 (단위테스트 대상)
      calc.test.ts
    discount/                # 할인율 계산기
      DiscountScreen.tsx
      calc.ts
    travel-expense/          # 여행 경비 계산기
      TravelExpenseScreen.tsx
      calc.ts
    split-bill/               # N빵 계산기
      SplitBillScreen.tsx
      calc.ts
    salary/                    # 월급/시급 계산기
      SalaryScreen.tsx
      calc.ts
  components/                 # 공통 컴포넌트
    NumberInput.tsx
    ResultCard.tsx
    CategoryCard.tsx
    ModeTabs.tsx              # 계산 모드 전환용 세그먼트 컨트롤
    ShareResultButton.tsx
  utils/
    format.ts                 # 천단위 콤마, 원단위 반올림 등 공통 포맷
  theme/
    colors.ts
    typography.ts
```

각 `features/*`는 화면(UI)과 `calc.ts`(순수 함수, 입출력만 있는 계산 로직)를 분리한다. 이렇게 하면:
- 계산 로직만 따로 단위테스트 가능
- 카테고리 하나를 갈아엎어도 다른 카테고리에 영향 없음
- 새 카테고리 추가가 "폴더 하나 추가"로 끝남

---

## 3. 디자인 가이드

- **배경**: 화이트(#FFFFFF) 기본, 섹션 구분용 연회색(#F2F4F6) 배경 카드
- **포인트 컬러**: 토스 블루 `#3182F6` (버튼, 강조 텍스트, 결과 하이라이트)
- **텍스트**: 본문 `#191F28`(진회색, 순검정 대신), 보조 텍스트 `#8B95A1`
- **컴포넌트 스타일**: 카드 라운드 16px, 그림자는 최소화(테두리 없는 flat 카드 + 옅은 elevation)
- **폰트**: 시스템 기본 폰트 우선 (Apps in Toss가 자체 지원하는 토스 폰트 토큰이 있다면 그것 사용, PC 작업 시 TDS 문서에서 최신 토큰 확인)
- **인터랙션 패턴**:
  - 홈 화면: 2열 그리드의 카테고리 카드 (아이콘 + 이름 + 한줄 설명)
  - 계산기 화면: 상단부터 입력 필드 나열 → 화면 하단 sticky 영역에 결과 카드 고정 표시 (입력하는 즉시 실시간 계산/반영)
  - 계산 모드가 여러 개인 카테고리(할인율, 월급 등)는 상단 세그먼트 탭으로 모드 전환
  - 결과 화면에 "공유하기"(캡처/공유) 버튼 공통 배치

---

## 4. 정보 구조

```
홈 (카테고리 그리드)
 ├─ 원가계산기
 ├─ 할인율 계산기
 ├─ 여행 경비 계산기
 ├─ N빵 계산기
 └─ 월급/시급 계산기
```

향후 확장 후보(초기 범위 아님, 구조만 열어둠): 부가세 계산기, 대출이자 계산기, 퇴직금 계산기.

---

## 5. 카테고리별 기능 명세

### 5-1. 원가계산기 (단품 원가/마진/판매가)

**입력**
- 재료비 합계 (원)
- 부자재/포장비 (원, 선택)
- 인건비 배분액 (원, 선택)
- 기타 비용 (원, 선택)
- 모드 전환: [마진율로 판매가 구하기] / [판매가로 마진율 구하기]
  - 모드 A: 목표 마진율(%) 입력
  - 모드 B: 판매가(원) 입력

**계산 로직**
- 총원가 = 재료비 + 부자재비 + 인건비 + 기타비용
- 모드 A: 판매가 = 총원가 / (1 − 마진율/100)  *(마진율 100% 이상 입력 방지, 에러 안내)*
- 모드 B: 마진율(%) = (판매가 − 총원가) / 판매가 × 100
- 마진액 = 판매가 − 총원가
- 부가세 포함가(선택 토글) = 판매가 × 1.1

**출력**: 총원가, 판매가, 마진액, 마진율(%), (옵션) 부가세 포함 판매가

---

### 5-2. 할인율 계산기

**모드**
- 모드 A: 정가 + 할인가 → 할인율, 할인금액
- 모드 B: 정가 + 할인율(%) → 할인가, 할인금액
- 모드 C: 중복 할인 (예: 20% + 10%) → 최종가, 총 할인율(단순 합산이 아닌 순차 적용 결과)

**계산 로직**
- 모드 A: 할인금액 = 정가 − 할인가, 할인율(%) = 할인금액/정가×100
- 모드 B: 할인가 = 정가 × (1 − 할인율/100)
- 모드 C: 최종가 = 정가 × (1−r1/100) × (1−r2/100) × …, 실질 할인율 = (정가−최종가)/정가×100

**출력**: 할인가(또는 할인율), 절약 금액, 절약률(%)

---

### 5-3. 여행 경비 계산기

**입력**
- 여행 인원 수
- 여행 일수 (선택, 1일 평균 산출용)
- 항목별 총 비용: 숙박 / 교통 / 식비 / 관광·액티비티 / 기타 (각각 원화 입력)
- (선택) 외화 지출 항목 + 적용 환율 → 원화 환산 후 합산

**계산 로직**
- 총 경비 = 항목별 합계
- 인당 경비 = 총 경비 / 인원 수
- 1일 평균 경비 = 총 경비 / 여행 일수 (일수 입력 시)
- 항목별 비중(%) = 각 항목 / 총 경비 × 100

**출력**: 총 경비, 인당 경비, (선택) 1일 평균, 항목별 비중 리스트(막대 또는 텍스트 비율)

> 역할 분리: 여행 경비 계산기는 "예산 파악/총량 계산"용이고, 인원별 실제 정산(누가 더 내고 덜 냈는지)은 N빵 계산기의 차등 정산 모드를 안내/연결한다.

---

### 5-4. N빵 계산기 (더치페이/정산)

**모드**
- 모드 A: 단순 N빵 — 총액 ÷ 인원수, 10원/100원 단위 반올림 처리, 나머지(잔돈)는 "1명이 더 낸다" 식으로 자동 배정
- 모드 B: 차등 정산 — 인원별 실제 지출 금액을 각각 입력 → 평균 대비 초과/부족분을 계산해 "누가 누구에게 얼마를 보내야 하는지" 최소 송금 횟수로 정산

**계산 로직 (모드 B)**
1. 평균 지출 = 총 지출 합 / 인원수
2. 각자 잔액 = 본인 지출 − 평균 지출 (양수면 받을 돈, 음수면 낼 돈)
3. 받을 사람/낼 사람 리스트를 만들어 금액이 큰 순서대로 매칭하며 송금 내역 생성 (그리디 매칭으로 송금 건수 최소화)

**출력**
- 모드 A: 인당 금액, 잔돈 처리 안내
- 모드 B: "OOO → ◇◇◇ : N원" 형태의 정산 리스트

---

### 5-5. 월급/시급 계산기

**모드**
- 모드 A: 시급 → 월 예상 급여 (시급 × 1일 근무시간 × 주 근무일수 × 4.345주, 주휴수당 포함 여부 토글)
- 모드 B: 연봉 → 월급/시급 환산
- 모드 C: 실수령액 근사 계산 — 4대보험(국민연금 4.5%, 건강보험 3.545%, 장기요양보험 건강보험료의 12.95%, 고용보험 0.9%) 공제 + 소득세·지방소득세 간이 근사 차감

**출력**: 세전 금액, 4대보험 공제 합계, 세금(근사), 실수령액(근사)

> 주의 문구 필수: "실제 세액은 부양가족 수, 간이세액표 등에 따라 달라질 수 있으며 본 결과는 참고용입니다." — 화면 내 명시.

---

## 6. 공통 컴포넌트

| 컴포넌트 | 역할 |
|---|---|
| `CategoryCard` | 홈 화면 카테고리 진입 카드 (아이콘/제목/설명) |
| `NumberInput` | 천단위 콤마 자동 포맷, 원화 단위 표시, 숫자 키패드 |
| `ModeTabs` | 계산 모드 세그먼트 탭 |
| `ResultCard` | 하단 고정 결과 표시 카드, 강조 숫자는 토스 블루 |
| `ShareResultButton` | 결과 캡처/공유 |

## 7. 데이터 저장

- 로그인/회원가입 없음. 모든 입력값은 세션 내 메모리 상태만 사용 (계산기 특성상 이력 저장 필요성 낮음).
- 필요 시 "최근 계산 3건" 정도만 로컬(AsyncStorage)에 저장하는 것을 2차 확장으로 고려 (기존 원가계산기 개인정보처리방침과 동일하게 "로컬 저장, 서버 미전송" 원칙 유지).

## 8. PC에서 진행할 개발 로드맵

1. **프로젝트 셋업**: Apps in Toss CLI로 React Native 프로젝트 생성, `appsInToss` 플러그인 설정(앱 이름, primary color `#3182F6`, 아이콘), TDS React Native 패키지 설치
2. **공통 레이어**: `theme/`, `components/`, `utils/format.ts` 먼저 구현 + 홈 화면(카테고리 그리드)
3. **카테고리 구현 순서** (구현 난이도/독립성 기준 추천 순서):
   1. N빵 계산기 (모드 A 먼저, 모드 B는 다음 스프린트)
   2. 할인율 계산기
   3. 월급/시급 계산기
   4. 여행 경비 계산기
   5. 원가계산기
4. **약관/개인정보처리방침**: 통합계산기 전용 문서 신규 작성 (기존 `cost-calc-privacy.html`, `cost-calc-terms.html` 형식 참고, 서비스명만 교체)
5. **심사 제출**: 앱인토스 개발자센터 심사 프로세스 진행

## 9. 참고 자료

- 앱인토스 개발자센터: https://developers-apps-in-toss.toss.im/
- React Native 시작 가이드: https://developers-apps-in-toss.toss.im/tutorials/react-native.html
- TDS 컴포넌트 문서: https://developers-apps-in-toss.toss.im/design/components.html
- 예제 레포: https://github.com/toss/apps-in-toss-examples

> 위 문서 도메인은 이 세션의 네트워크 정책상 직접 열람이 막혀 있어 최신 세부 스펙(정확한 색상 토큰, CLI 명령어 등)은 PC 세션에서 위 링크로 다시 확인 후 시작할 것.

## 10. PC 세션 시작 체크리스트

- [ ] 이 브랜치(`claude/mobile-to-pc-transfer-p4ipjo`) pull
- [ ] 위 개발자센터 링크에서 최신 React Native 셋업 가이드 확인 (CLI 명령어, `appsInToss` 플러그인 옵션 최신화)
- [ ] TDS 컴포넌트 문서에서 정확한 컬러 토큰/타이포그래피 토큰 확인 후 `theme/colors.ts` 반영
- [ ] 프로젝트 스캐폴딩 → 홈 화면 → 카테고리 1개(N빵)부터 MVP 구현
