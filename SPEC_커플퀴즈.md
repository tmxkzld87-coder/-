# 커플 퀴즈 — 초안

- 작성일: 2026-09-24
- 기반: 찐친 퀴즈 템플릿(`C:\Users\User\jjinchin-quiz`, 복제 절차는 그 README 체크리스트). 코드 구조·링크 인코딩·광고·검수 규칙은 [SPEC_찐친퀴즈.md](SPEC_찐친퀴즈.md)와 동일하고, 이 문서는 **바뀌는 값만** 정한다.
- 착수 조건: 찐친 퀴즈 검수(`20260924-2`) 결과 확인 후. 반려되면 그 수정을 템플릿에 먼저 반영하고 복제한다.

## 1. 콘솔 등록값

| 항목 | 값 |
|---|---|
| title | 커플 퀴즈 |
| titleEn | Couple Quiz |
| appName | `couple-quiz` (전역 유니크라 선점돼 있으면 `jjinchin-couple-quiz`) |
| description(20자 이내) | 연인이 나를 얼마나 아는지 퀴즈로 |
| 카테고리 | 생활 > 콘텐츠(3834) > 테스트(72) |
| 키워드 | 커플, 커플퀴즈, 연애, 커플테스트, 연인 |

## 2. `src/brand.ts` 값

| 키 | 값 |
|---|---|
| `audience` | 연인 → "연인에게 보내기", "연인에게 보일 내 이름" |
| `colors.primary` / `primaryBg` / `background` | `#F2545B` / `#FFECEC` / `#FBF5F5` (찐친 분홍과 구분되는 코랄 레드) |
| `home.emoji` | 💑 |
| `home.title` | 내 연인은 나를\n얼마나 알까? |
| `home.subtitle` | 나에 대한 5문제를 만들어 보내고\n우리 사이 이해도를 확인해보세요 |
| `honorific` | `false` → "민지가 좋아하는", "민준이는" (님 없이 부르는 말투) |
| `playIntroTitle` | `${name('을')}\n얼마나 알고 있나요?` → "민준이를 얼마나 알고 있나요?" |
| 등급 5 / 3~4 / 1~2 / 0 | 천생연분 💘 / 잘 맞는 커플 💕 / 알아가는 중 🌱 / 우리… 처음 만났나요? 😳 |
| 퀴즈 공유 | `${name('이')} 나에 대한 퀴즈를 냈어요 💌 몇 개나 맞힐 수 있을까요? 👉 {링크}` |
| 결과 공유 | `${name('')} 퀴즈 {점수}/5점! {등급} 우리도 해볼래? 👉 {링크}` |

- 아이콘: 코랄 레드 배경을 꽉 채운 정사각형 + 흰 하트 두 개가 겹친 모양 (찐친은 하트 하나 + 물음표)
- OG 이미지: 같은 톤, 문구 "내 연인은 나를 얼마나 알까?"
- 호칭: 템플릿의 `honorific: false` 모드로 "님" 없이 부른다. 받침 있는 이름은 "이"를 붙여 "민준이가, 민준이는"(2026-09-24 사용자 결정).

## 3. 문제 은행 초안 (40문제)

원칙: 연애 취향·생활 습관 위주로 가볍게. 성적 내용, 과거 연애, 외모 평가, 돈 액수처럼 민감하거나 싸움을 부를 수 있는 주제는 넣지 않는다(검수·연령등급 리스크).

1. `{이}` 제일 좋아하는 데이트는? — 맛집 탐방 / 카페 / 집 데이트 / 여행
2. 다툰 뒤 `{이}` 원하는 건? — 바로 사과 / 시간 갖기 / 맛있는 거 / 꼭 안아주기
3. `{이}` 제일 설레는 순간은? — 손잡을 때 / 연락 올 때 / 선물 받을 때 / 같이 웃을 때
4. `{이}` 좋아하는 애정 표현은? — 말로 표현 / 스킨십 / 선물 / 챙겨주기
5. 기념일, `{은}`? — 꼭 챙긴다 / 적당히 / 별로 신경 안 씀
6. `{이}` 받고 싶은 선물은? — 손편지 / 꽃 / 필요한 물건 / 현금
7. 연락, `{은}`? — 자주 하고 싶다 / 할 말 있을 때만 / 전화가 좋다 / 만나서 얘기
8. `{이}` 더 좋아하는 데이트 시간은? — 낮 / 밤
9. `{은}` 커플템을? — 좋아한다 / 부담스럽다 / 몰래 하나쯤
10. `{이}` 가장 가보고 싶은 여행지는? — 제주도 / 일본 / 유럽 / 동남아
11. `{이}` 서운할 때 하는 행동은? — 말로 한다 / 티를 낸다 / 참는다 / 잠수
12. 영화관 데이트에서 `{은}`? — 팝콘 필수 / 음료만 / 아무것도 안 먹음
13. `{이}` 좋아하는 데이트 메뉴는? — 고기 / 파스타 / 회·초밥 / 떡볶이
14. `{이}` 반한 포인트는? — 외모 / 성격 / 목소리 / 웃음
15. 사진 찍을 때 `{은}`? — 커플 사진 좋아함 / 찍어주는 게 좋음 / 사진 별로
16. `{이}` 제일 싫어하는 연애 습관은? — 약속에 늦기 / 연락 두절 / 거짓말 / 무관심
17. `{은}` 질투가? — 많다 / 적당하다 / 거의 없다
18. `{이}` 더 좋아하는 데이트 장소는? — 바다 / 산 / 도시 / 집
19. 비 오는 날 데이트, `{은}`? — 오히려 좋아 / 집에 있고 싶어
20. `{이}` 기분 풀리는 한마디는? — 미안해 / 고마워 / 사랑해 / 밥 먹자
21. 연애에서 `{이}` 제일 중요하게 생각하는 건? — 신뢰 / 대화 / 배려 / 재미
22. 데이트 계획, `{은}`? — 내가 짜는 편 / 맡기는 편 / 그때그때
23. `{의}` 잠버릇은? — 조용히 잠 / 이리저리 뒤척임 / 잠꼬대 / 나도 모름
24. `{이}` 제일 좋아하는 계절 데이트는? — 봄 꽃구경 / 여름 바다 / 가을 단풍 / 겨울 눈
25. `{은}` 연인이 옷 골라주는 게? — 좋다 / 부담스럽다
26. 아플 때 `{이}` 원하는 건? — 옆에 있어주기 / 약 사다 주기 / 혼자 쉬기 / 연락만
27. `{은}` 서프라이즈 이벤트를? — 좋아한다 / 부담스럽다
28. 데이트 끝나고 `{은}`? — 헤어지자마자 연락 / 집 가서 연락 / 다음 날 연락
29. `{이}` 더 좋아하는 호칭은? — 이름 / 애칭 / 자기 / 여보
30. 커플 여행에서 `{은}`? — 계획파 / 즉흥파
31. `{이}` 제일 좋아하는 스킨십은? — 손잡기 / 포옹 / 팔짱 / 머리 쓰다듬기
32. 연인이 약속에 늦으면 `{은}`? — 괜찮다 / 조금 서운 / 많이 서운
33. 연애 초반 `{이}` 제일 떨렸던 순간은? — 첫 만남 / 첫 데이트 / 고백 / 첫 여행
34. `{은}` 연인의 친구들을 만나는 게? — 좋다 / 적당히 / 불편하다
35. `{이}` 제일 연락하고 싶은 때는? — 아침 / 점심 / 퇴근길 / 자기 전
36. `{이}` 좋아하는 데이트 간식은? — 아이스크림 / 붕어빵 / 커피 / 탕후루
37. 요리, `{은}`? — 해주고 싶다 / 받고 싶다 / 같이 하고 싶다
38. `{이}` 좋아하는 데이트 옷차림은? — 편한 옷 / 꾸민 옷 / 커플룩
39. 다투면 `{은}`? — 먼저 연락한다 / 기다린다
40. `{이}` 꿈꾸는 우리의 미래는? — 여행 다니기 / 반려동물 키우기 / 조용한 일상 / 같이 일하기

## 4. 작업 순서 (착수 후)

1. `jjinchin-quiz` 복사 → `couple-quiz` 폴더, README 체크리스트 1~5 적용
2. 문제 은행 교체 후 `questions.test.ts`의 "40문제" 등 수치 검사 통과 확인, `npx jest` / `tsc` / 빌드
3. 아이콘·OG 이미지 생성 → 콘솔 등록(`iconUploadId`) → 광고 그룹 2개("커플퀴즈 배너", "커플퀴즈 오답확인 리워드")
4. 테스트 광고 ID로 빌드 → 업로드 → 테스트 푸시 → 사용자 확인 → 실제 ID로 교체·`.ait` 스캔 → 검수 제출
