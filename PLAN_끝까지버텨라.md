# 끝까지 버텨라 (토스 앱인토스 미니게임) 기획서

플랫폼: Apps in Toss (앱인토스)
작성일: 2026-08-27
사업자: 육퇴못한파더 작업실 (기존 미니앱들과 동일 사업자, workspace id 77253)
장르: 하이퍼 캐주얼 생존형 아케이드

> 이 문서는 사용자가 대화로 직접 작성한 상세 기획안을 그대로 옮긴 것이다. Claude Code 작업은 반드시 아래 "13. 개발 순서(Phase)"를 따라 **단계별로** 진행하며, 한 번에 전체를 구현하지 않는다.

---

## 1. 프로젝트 개요

- **게임명**: 끝까지 버텨라
- **폴더/appName**: `kkeutkkaji-beotyeora`
- **개발 환경**: Claude Code 기반, 모바일 웹/미니앱(Granite/React Native) 환경, 터치 조작, 세로 화면. 별도 로그인 절차 최소화, 토스 환경에 자연스럽게 연결.
- **핵심 목표**: 한 판을 짧게 플레이하고 바로 재도전할 수 있는 구조. 사용자가 "한 판만 더"를 느끼게 만드는 것이 최우선.

핵심 루프:
```
게임 시작 → 장애물 회피 → 점수 상승 → 난이도 상승 → 사망
→ 점수/랭킹 확인 → (광고 시청 후 1회 부활 | 재시작) → 다시 도전 → 최고 기록 갱신
```

---

## 2. 핵심 게임 컨셉 & 조작

- 화면 안의 플레이어가 계속 등장하는 장애물을 피한다. 생존 시간이 길수록 난이도 상승.
- 위험한 상황을 가까스로 피하면 추가 점수(근접 회피).
- 조작: **화면 드래그 → 플레이어가 손가락을 따라 이동**. 화면 밖으로 못 나가게 제한. 장애물과 충돌 시 즉시 게임오버.
- 가상 조이스틱 없이 직관적인 드래그 조작만 사용.

---

## 3. 게임 플레이 흐름

- 홈 화면에서 [게임 시작] → 바로 3초 카운트다운(`3 2 1 GO!`) → 게임 시작.
- 긴 설명/튜토리얼 없음.

---

## 4. 장애물 시스템

기본 장애물 6종 (전부는 Phase 1에 넣지 않고 Phase 1은 1~2종, Phase 2에서 확장 — 13절 참고):
1. 직선 이동 장애물
2. 위에서 떨어지는 장애물
3. 좌우 이동 장애물
4. 플레이어를 추적하는 장애물
5. 일정 시간 후 폭발하는 장애물
6. 다중 동시 등장 장애물

원칙: **완전히 피할 수 없는 상황은 절대 발생 금지.** 모든 장애물은 최소 회피 공간과 반응 시간을 보장해야 한다.

---

## 5. 난이도 시스템

시간 경과에 따라 점진적으로 어려워진다 (여러 요소를 조합, 속도만 올리지 않는다):

| 구간 | 난이도 | 특징 |
|---|---|---|
| 0~20초 | 매우 쉬움 | 장애물 속도 낮음, 수 적음, 회피 공간 충분 |
| 20~40초 | 쉬움 | 속도/수 증가 |
| 40~60초 | 보통 | 복합 패턴 등장, 간격 감소 |
| 60~90초 | 어려움 | 빠른 장애물, 여러 방향 동시 등장 |
| 90초~ | 매우 어려움 | 생성 빈도/속도/복합 패턴 모두 증가 |

---

## 6. 점수 시스템

- 기본 점수 = 생존 시간 × 난이도 배율 (지속 증가)
- 근접 회피(NEAR MISS): +50 등 추가 점수
- 연속 회피 콤보(COMBO x2, x3, x4...): 콤보 높을수록 획득량 증가
- 게임 화면에 현재 점수 항상 표시

---

## 7. 게임오버 화면

표시 정보: 이번 점수, 생존 시간, 최고 점수, 현재 랭킹, 최고 랭킹, 바로 위 플레이어 점수 차이.
버튼: `[ 광고 보고 부활 ]` `[ 다시 시작 ]` `[ 랭킹 보기 ]`

예시:
```
GAME OVER
8,420점 · 생존시간 01:42
현재 순위 #127 · 최고 기록 #93 · 내 최고점수 12,420
현재 126위와 30점 차이!
```

(랭킹/최고 기록 관련 항목은 Phase 3 이전에는 로컬 데이터만으로 표시)

---

## 8. 부활 시스템

- 게임오버 → 보상형 광고 시청 → 부활 → 2~3초 무적(시각적으로 명확히 표시) → 게임 재개.
- **한 게임당 광고 부활은 기본 1회만 허용** (무제한 부활 금지, 점수 경쟁 공정성).

---

## 9. 광고 수익화 구조 (Phase 4/5)

- 보상형 광고: 게임오버 화면 `[ 광고 보고 부활 ]`, 시청은 절대 강제하지 않음 — `[ 다시 시작 ]` 선택도 항상 가능.
- 전면 광고: 매 게임 종료마다 무조건 노출하지 않음. `AD_INTERVAL = 3`(3게임마다) 같은 조절 가능한 구조. 광고 로직은 게임 로직과 완전히 분리.
- 실제 앱인토스 광고 정책/최신 SDK 정책은 Phase 4 진입 시 재확인 후 최종 적용.

---

## 10. 랭킹 시스템 (Phase 3)

- **전체 랭킹**: 최고 점수 기준 순위표
- **주간 랭킹**: 매주 초기화, 주간 1위 특별 표시
- **내 주변 랭킹**: 내 순위 기준 위/아래 플레이어 노출 (가장 중요) — "30점만 더 얻으면 한 명 추월" 감정 유도

---

## 11. 오늘의 도전 (Phase 3)

- 매일 모든 사용자가 동일한 장애물 패턴(시드 고정)으로 플레이하는 별도 모드, 별도 랭킹.
- 패턴은 매일 새로 교체.

---

## 12. 화면 구성

### 홈 화면
```
끝까지 버텨라
오늘의 1위 82,430 · 내 최고점수 12,420 · 내 최고순위 #127
[ 게임 시작 ]  (중앙 큰 버튼)
[ 오늘의 도전 ] [ 랭킹 ] [ 설정 ]  (하단)
```
앱 실행 후 3초 안에 게임을 시작할 수 있어야 한다. (Phase 1에서는 오늘의 1위/랭킹 항목 대신 "내 최고점수/최고 생존시간"만 로컬로 표시)

### 게임 화면
상단: 점수, 생존시간. 중앙: 플레이어, 장애물. 하단 버튼 최소화. 플레이 중 광고/팝업으로 방해 금지.

### 게임오버 화면
7절 참고. Phase 1에서는 순위 관련 항목 제외, 점수/생존시간/최고기록/재시작만.

---

## 13. 개발 순서 (Phase) — Claude Code 작업 단위

**처음부터 전체를 한 번에 구현하지 않는다.** 각 Phase 완료 후 실제 모바일 환경(granite dev 또는 빌드)에서 테스트한다.

### PHASE 1 — 게임 프로토타입 (지금 진행)
광고·랭킹 없이 순수 게임성만.
- 홈 화면 (게임 시작 버튼, 최고점수/최고 생존시간 로컬 표시)
- 게임 시작 (3-2-1-GO 카운트다운)
- 플레이어 드래그 이동 (화면 경계 제한)
- 장애물 생성 (직선 이동형 1종으로 시작, 위에서 떨어지는 유형 1종 추가 — 2종)
- 충돌 판정 (회피 불가 상황 방지 로직 포함)
- 점수(생존시간 기반) + 생존시간 표시
- 난이도 증가 (5절 표의 처음 2~3구간만: 0~20초, 20~40초, 40초~)
- 게임오버 화면 (점수/생존시간/최고기록/다시시작)
- 재시작
- 로컬 저장: 최고 점수, 최고 생존 시간, 총 플레이 횟수

목표 질문: **"실제로 재미있는가?"**

### PHASE 2 — 게임성 강화
장애물 종류 확장(나머지 4종), 근접 회피(NEAR MISS), 콤보, 랜덤 이벤트(16절), 난이도 5구간 전체 밸런싱, 게임오버 연출(NEW RECORD!), 기록 갱신 연출.

### PHASE 3 — 경쟁 시스템
전체 랭킹, 주간 랭킹, 내 주변 랭킹, 오늘의 도전(고정 시드), 순위 상승 연출. (서버/API 필요 — 백엔드 방식은 이 Phase 시작 시 별도 결정)

### PHASE 4 — 앱인토스 연동
앱인토스 환경 대응, 사용자 식별, 랭킹 서버 연동, 앱인토스 광고 SDK(보상형/전면), 필요 시 푸시. **최신 공식 개발 문서/SDK 반드시 재확인 후 구현.**

### PHASE 5 — 수익화 최적화
광고 무작정 증가 금지. 부활 광고 선택률·전면 광고 이탈률·세션 길이·재방문율을 측정 후 조정. 플레이 시간 감소 시 광고 빈도 축소.

---

## 14. 랜덤 이벤트 (Phase 2)

SPEED UP / GOLD TIME(점수 2배) / CHAOS(패턴 변경) / SAFE TIME(장애물 감소) / BLACKOUT(시야 제한). **운으로 점수가 갈리지 않도록, 전부 실력으로 대응 가능한 수준으로 설계.**

---

## 15. 데이터 저장

최소 저장 항목: 최고 점수, 최고 생존 시간, 최고 순위, 총 플레이 횟수, 총 생존 시간, 광고 부활 횟수, 마지막 플레이 날짜, 오늘의 도전 점수, 주간 최고 점수.
Phase 1에서는 최고 점수/최고 생존 시간/총 플레이 횟수만 AsyncStorage에 저장. 나머지는 해당 Phase에서 추가.
사용자 행동 데이터 추적이 쉽도록 저장 로직은 별도 모듈(`src/storage/`)로 분리.

---

## 16. 핵심 지표 (Phase 3~5 이후 의미 있음)

가장 중요: ① 하루 평균 플레이 횟수 ② 게임오버 후 재시작률 ③ 보상형 광고 선택률 ④ D1/D7 재방문율.

---

## 17. UI 디자인 방향

- 세로형 모바일, 큰 숫자, 큰 버튼, 최소 텍스트, 높은 가독성
- 게임 중 불필요한 UI 제거, 애니메이션은 빠르고 명확하게
- 장애물과 플레이어가 확실히 구분되는 색/형태
- 토스 미니앱에 자연스럽게 보이도록 과도하게 무거운 게임 UI 지양

---

## 18. 개발 시 반드시 지켜야 할 원칙

1. 게임 시작까지 최대한 짧게. 2. 첫 플레이부터 재미. 3. 충돌 판정은 공정해야 함(히트박스는 시각적 크기보다 살짝 작게). 4. 피할 수 없는 장애물 생성 금지. 5. 랜덤 요소로 운빨 게임이 되지 않게. 6. 광고로 플레이 흐름 끊지 않기. 7. 보상형 광고는 명확한 사용자 선택. 8. 부활은 게임당 기본 1회. 9. 랭킹 조작 방지 고려(Phase 3+). 10. 점수 계산은 클라이언트만 신뢰하지 않고 서버 검증 구조 고려(Phase 3+). 11. 네트워크 끊겨도 게임 플레이 자체는 가능. 12. 저사양 환경에서도 부드럽게 동작. 13. 게임 로직과 광고/랭킹/API 로직 분리. 14. 장애물/게임모드 추가하기 쉬운 구조. 15. 앱인토스 최신 정책/SDK 문서 확인 후 구현(Phase 4+).

---

## 19. Phase 1 기술 아키텍처

- **스택**: 기존 미니앱들과 동일 — `@apps-in-toss/framework` + Granite(React Native) + TypeScript. 신규 네이티브 의존성(react-native-skia, reanimated 등)은 Phase 1에서는 추가하지 않는다 — 하이퍼 캐주얼 특성상 동시 오브젝트 수가 적어(수 개~10여 개) 순수 RN View + JS 게임 루프로 충분한 성능이 나온다. 성능 이슈가 실측되면 Phase 2 이후 스킬 판단으로 도입 검토.
- **게임 루프**: `requestAnimationFrame` 기반 단일 루프에서 매 프레임 (a) 오브젝트 위치 갱신 (b) 충돌 판정 (c) 점수/생존시간 갱신을 수행하고 React state로 렌더링. 루프/충돌/점수 로직은 `src/game/` 아래 순수 함수로 분리해 UI(`GameScreen.tsx`)와 독립적으로 테스트 가능하게 만든다.
- **조작**: RN `PanResponder`로 드래그 좌표를 플레이어 x/y에 반영, 화면 경계로 clamp.
- **충돌 판정**: 원형/사각형 히트박스 기반 단순 거리·겹침 판정 (`src/game/collision.ts`), 시각 크기보다 살짝 작은 히트박스로 공정성 확보.
- **장애물 스폰**: `src/game/spawner.ts` — 난이도 구간별 파라미터(속도, 스폰 간격, 동시 개수)를 테이블로 관리해 5절 난이도 표를 그대로 코드화. 스폰 시 항상 화면 폭 기준 최소 회피 폭을 보장하는 룰 포함.
- **폴더 구조**:
  ```
  src/
    pages/
      index.tsx          # 홈 화면
      game.tsx           # 게임 화면 (카운트다운 포함)
    game/
      loop.ts            # requestAnimationFrame 루프, 상태 업데이트
      spawner.ts          # 난이도별 장애물 생성 로직
      collision.ts        # 충돌 판정
      types.ts            # Obstacle, Player, GameState 타입
    components/
      Player.tsx
      Obstacle.tsx
      Hud.tsx              # 점수/생존시간 표시
      CountdownOverlay.tsx
      GameOverPanel.tsx
    storage/
      records.ts           # 최고점수/최고생존시간/플레이횟수 AsyncStorage
  ```
- **테스트**: `collision.ts`, `spawner.ts`의 난이도 파라미터 계산은 순수 함수 단위 테스트로 검증(jest). 게임 루프/렌더링은 실기기(모바일 웹뷰) 수동 테스트로 확인.

---

## 20. Phase 1 조작 방식 변경 (2026-08-27)

첫 테스트 푸시 플레이 후 드래그 이동 조작이 잘 안 맞는다는 피드백을 받아, 조작 방식을 아래와 같이 전면 교체했다. 2절/4절/12절의 "드래그 이동"·"위에서 떨어지는/좌우 이동 장애물" 서술은 이 절로 대체된다.

- **조작**: 좌우 이동 없음. 화면 아무 곳이나 터치하면 캐릭터가 점프. 공중에서 한 번 더 터치하면 2단 점프(더 높이 뜸). 착지 전까지 최대 2회.
- **장애물**: 화면 오른쪽에서 왼쪽으로 흘러오는 바닥 장애물 2종 — 낮은 장애물(1단 점프로 회피), 높은 장애물(2단 점프 필요).
- **연출**: 캐릭터는 화면 왼쪽 고정 위치에 머물고, 장애물과 배경이 왼쪽으로 흘러가며 전진하는 느낌을 낸다. 배경은 원경/근경 2겹 패럴럭스로 스크롤되고, 난이도 구간(0~20/20~40/40초~)이 바뀔 때마다 배경 테마(새벽→노을→밤)도 함께 바뀐다.
- **기술**: `src/game/physics.ts`(중력·점프 임펄스), `src/game/render.ts`(바닥 기준 좌표 → 화면 좌표 변환)를 신설. `collision.ts`는 원(캐릭터)-사각형(장애물) 판정으로 재작성. PanResponder 드래그 코드는 제거하고 화면 탭 감지(`onStartShouldSetResponder`/`onResponderGrant`)로 교체.

## 21. Phase 2 구현 내역 (2026-08-27)

러너 구조(20절) 위에 아래를 추가했다.

- **장애물 4종**: `low`(1단 점프로 회피) · `tall`(2단 점프 필요) · `wide`(더 넓은 낮은 장애물, 점프 타이밍이 더 빡빡함) · `overhead`(천장에 매달린 장애물 — 점프하지 않고 그대로 지나가야 함, 뛰면 반드시 충돌). 기존 기획서 4절의 "위에서 떨어지는/좌우 이동/추적형/폭발형" 등은 드래그-회피 방식 전용 설계라 러너 구조에 맞게 이 4종으로 재해석했다.
- **근접 회피(NEAR MISS) + 콤보**: 장애물을 스칠 듯이(히트박스는 안 닿지만 26px 이내로) 통과하면 근접 회피로 인정, 기본 +50점. 2.5초 안에 연속으로 근접 회피하면 콤보가 올라가고(COMBO x2, x3...) 보너스 점수도 늘어난다(50 + (콤보-1)×15). 콤보 창이 지나면 콤보는 0으로 리셋.
- **난이도 5구간 전체 구현**: 0~20 / 20~40 / 40~60 / 60~90 / 90초~. 배경 테마는 새벽(0~40초) → 노을(40~90초) → 밤(90초~) 3종을 재사용해 매칭.
- **랜덤 이벤트**: GOLD TIME(점수 2배, 5초)만 유지. SAFE TIME(장애물 완화)은 사용자 피드백으로 삭제 — 게임을 쉽게 만드는 이벤트보다 순수 보상형 이벤트만 남기는 방향. 15~25초 간격으로 무작위 발생. 기획서 16절의 SPEED UP/CHAOS/BLACKOUT은 불공정 위험이 있어 Phase 2 MVP에서는 제외.
- **속도 가속 곡선 강화(2026-08-27)**: 구간별 속도를 균등 증가(+60/구간)에서 뒤로 갈수록 증가폭이 커지는 가속 곡선(260→340→450→600→800, 증가폭 +80/+110/+150/+200)으로 변경 — 난이도가 오를수록 체감 속도가 더 가파르게 빨라지도록.
- **이동 거리(m) 표시(2026-08-27)**: 배경 패럴럭스 스크롤량(`backgroundOffset`, px)을 `PX_PER_METER=50`으로 환산해 "OOOm"으로 표시(`src/game/distance.ts`). 게임 화면 상단 중앙에 항상 표시하고, 게임오버 화면 생존시간 옆에도 같이 보여준다. 판정에는 쓰이지 않는 순수 연출용 값.
- **버그 수정**: GOLD TIME 배너를 Hud와 플레이 영역 사이의 별도 flex 줄로 넣었더니, 배너가 나타날 때 플레이 영역 박스가 실제로 줄어드는데 캐릭터/장애물 좌표 계산은 최초 측정한(더 큰) 크기를 그대로 써서 화면 아래쪽이 잘려 보이는 문제가 있었다. 배너를 플레이 영역 내부의 절대 위치 오버레이로 옮겨 레이아웃 크기에 영향을 주지 않도록 수정.
- **연출**: 상단 HUD에 콤보 배지, 화면 상단에 진행 중인 이벤트 배너, 근접회피/콤보/이벤트 시작 시 화면 중앙에 짧은 토스트 텍스트. 게임오버 화면의 NEW RECORD! 연출은 Phase 1에서 이미 구현됨.
- **기술**: `src/game/scoring.ts`(근접회피 점수 계산), `src/game/events.ts`(랜덤 이벤트 상수/추첨) 신설. `collision.ts`에 `isNearMiss` 추가, 장애물 판정을 `bottom`/`height` 구간 기반으로 일반화(바닥 장애물은 위쪽 가장자리를, 공중 장애물은 아래쪽 가장자리를 후하게 깎는 방식). 순수 함수 테스트 42개로 확장.

## 22. 참고 자료

- 앱인토스 개발자센터: https://developers-apps-in-toss.toss.im/
- 예제 레포: https://github.com/toss/apps-in-toss-examples
- 기존 프로젝트 설정 템플릿: `chuck-chuck-calculator`, `menu-cost-calculator` (package.json, granite.config.ts 등 그대로 복사해 재사용)
